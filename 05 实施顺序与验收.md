---
tags:
  - fruit-dronefly
  - 工程审查
类型: 工程改动
工程状态: 阶段 0-2、4 部分通过；CUDA 采样专项通过
文档审查状态: 已完成本轮静态审查
代码实施状态: 基线 acfdfcf 加未提交 F01 模块；首版测试图及相机闭环未实施
运行验证状态: 原有专项及 F01 独立探针通过；完整飞行策略与相机闭环验收仍待完成
核查日期: 2026-09-23
文档修订: 独立接手版（最小骨架与FlyDrones扩展）
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 实施顺序与验收

## 2026-09-23 F01 当前进展

F01 固定视频、冻结状态、读出 PPO 与导出已通过；相机避障闭环未接入。源码基线 `acfdfcf` 加未提交 F01 工作树，不能仅凭 HEAD 复现；12 个受测文件 SHA-256 见 [[05 实施顺序与验收#2026-09-23 F01：冻结视觉独立探针与读出验证]]。环境为从 `env_isaaclab` 克隆的 `fruit_dronefly`。本页既有首版与历史验收记录保留，独立读出验证不替代测试图、完整飞行策略或相机闭环验收。


[[07 工程上下文与接手约定|工程上下文与接手约定]] · [[README|阅读入口]]

[[00 改动总览|返回总览]] · [[01 仓库清理与任务收敛|清理范围]] · [[04 后续升级/00 升级依赖与实施边界|后续范围]]

> [!info] 本页是工程验收计划
> 图策略的工程验收尚未执行；本页新增 CUDA 采样、局部 reset 和 PPO smoke 专项证据。原 v0 兼容性已有部分实测，见 [[08 运行环境兼容性与版本方案]]；不能将其直接勾选为下面的新任务验收通过。


本轮 F01 实施前的历史复核（2026-09-23）：源码 HEAD 为 `acfdfcf`（Lee 陀螺补偿与退化姿态修复），`git status --short` 无输出。P03 理想执行模型专项已通过，证据见 [[05 实施顺序与验收#2026-09-23 P03：理想执行模型验证]]；提交状态核查不新增运行验证，不代表电机标定、PPO 重训或完整首版验收通过。

2026-09-23 F01 独立运行探针已通过，详见 [[05 实施顺序与验收#2026-09-23 F01：冻结视觉独立探针与读出验证]]。接下来保留首版图 Actor、图 checkpoint、play／完整策略导出和默认规模收尾，并准备 F1 相机任务、场景奖励与信息审计；正式 F1/F2 闭环仍须通过对应门槛。权重单独许可、方向调谐、stride 优化及有限解冻梯度路径未闭合；MaleCNS、嗅觉和部署后置。

## 阶段 0：基线与依赖核查

这是接手实施的第一步；本次仅维护文档，不执行下列工程操作。

1. 按 [[07 工程上下文与接手约定]] 确认实施目录、当前任务范围及参考目录；比对实际注册与文档事实。
2. 保存 Git 状态、现存源码清单、未跟踪文件及必要校验值，记录工作区已有修改；清理前必须另外保留可恢复的内容快照或完整基线提交，清单与哈希不能代替文件内容。被 .gitignore 排除但需要保留的旧模型、视频等另行归档；不得把未跟踪代码当作可丢弃文件。
3. 在实际训练解释器中记录路径和包版本，迁移前记录 `DroneFollow` 的实际来源；迁移后必须确认 `fruit_dronefly` 来自新源码仓库，且新入口不再导入旧包。依赖声明与安装事实分别记录，参见 [[06 本地代码库与复用清单#首版版本核查 B01／B02]]。
4. 核定 B01，登记 B02～B05 的核查入口及阶段要求；首版前置项未解决时，不用后续研究模块掩盖缺口。
5. 将结果、日期、证据路径写回本页和相关模块，之后进入阶段 1。记录事实不要求本轮自动升级依赖。

## 状态与证据维护

工程条目仍使用七项记录格式；文档审查、代码实施与运行验证分别维护。代码完成但未运行时标记“已实施／未执行”；运行失败记录结果和原因，不标为已通过。后续功能保持“后续未实施”，不会因首版通过而自动变成已实现。

证据记录至少包含：阶段／条目 ID、日期、源码或文件基线、运行环境、实际操作／命令、结果、日志或产物路径、限制与剩余事项。暂无产物时写“未生成”，不提供伪造 checkpoint 或日志链接。

## 顺序与证据

| 阶段        | 前置条件与操作                    | 通过条件                                   | 状态（2026-09-22）             |
| --------- | -------------------------- | -------------------------------------- | ---------------- |
| 0 基线记录    | 记录复制源码、未跟踪状态、依赖版本与安装路径     | 明确用的是新仓库源码                             | ✅ 基线提交 `b87f6a2`；`find_spec` 指向新树；Isaac Sim 5.1。⚠️ `pretrained/` 未纳入基线（见 P07） |
| 1 仓库收敛    | 按清理清单删除旧分支、改注册与命名          | 仅新 ID；无悬空导入；资产可加载                      | ✅ 唯一 `IsaacLab-FruitDronefly-v0`；宽域扫描 0 残留；USD 实跑加载成功。清理结果已提交 f7f085d；CUDA 优化已提交 f473d7b |
| 2 环境回归 | 保留 MLP；新 v0 小规模启动 | reset＋至少 100 步；19 维观测、3 维动作、有限值 | ✅ 本轮 4 环境、100 步真实物理回归通过。既有 64 环境训练为 960 步／环境的累计预算，期间允许 reset，不能据此认定单个 episode 完整运行 30 s |
| 3 测试图接入   | 新 Actor／Critic 与 runner 注册 | 图前向、梯度、mask 及 action distribution 合约正确 | ⬜ 未开始（首版计划内、当前未实施，P04） |
| 4 PPO 短运行 | 两种策略同环境少量更新 | 有限 loss／权重、参数变化、可保存 | 🟡 MLP：本轮 64 环境、3 次 PPO smoke 通过，checkpoint 重载确定性输出一致。图策略未实施 |
| 5 回放与导出   | 使用新 checkpoint             | 确定性输出一致；动作限幅一致；导出含 mask                | ⬜ `play.py` 与 JIT／ONNX 导出均未执行 |
| 6 审查回填 | 对照本次改动 | 代码位置、结果与限制一致 | ✅ 本轮 CUDA 优化、测试与限制已回填入口、总览、验收、接手、测试与接口页；后续模块保留原状态 |

## 关键验收场景

- [x] 原始任务名、PD／MPC 分支、删除的机器人／调参依赖没有残留在有效导入和 CLI 过滤中。（2026-09-21 宽域扫描 `.py/.toml/.yaml/.md/.sh`，0 处；`hover` 全词 0 处）
- [ ] 安装路径指向新仓库；task 子包和 USD 打包可用。（安装路径 ✅ editable 下子包与 USD 可用；**wheel 未构建**，`drone.usd` 是否进入产物无证据）
- [x] Actor/Critic 都为 19 维；Critic 已按 2026-09-21 用户要求改为当前无噪声、无延迟目标真值。专项观测和 4 环境实际输出检查通过，Actor 保持带噪延迟路径。
- [ ] 测试图有明确输入→输出路径，固定 mask 不可训练；多组非退化输入下梯度能通过有效路径。
- [ ] 非连接有效权重保持为零；图配置和映射可随 checkpoint 恢复。
- [ ] 动作零点、正负对称和逐轴饱和符合 5×tanh(a)；核对 wrapper 额外 clipping。（wrapper 侧 ✅ 闭合：`agent.yaml:7 clip_actions: null`，`train.py:199`／`play.py:169` 同读同一字段，无额外裁剪；映射实现见 `action.py:187`。**零点／对称／饱和的数值核对未执行**）
- [ ] heading 旋转的 +X／+Y 例子及往返变换正确，Z 不变。（Critic 专项已覆盖 +X／+Y 解析值；完整往返变换专项尚未执行）
- [x] 局部 reset 不污染其余环境；目标历史与动作延迟缓冲检查通过（4 环境、action_lag=2；重复／空／slice／全量 reset，含 approach 和手动 level 切换）。
- [x] 25 步无 reset／频繁 reset 命令对照通过（固定未来噪声），其他环境的目标与历史逐值一致；实际 composer 不变，下一物理步对照通过。限制和容差见下方证据。
      → 2026-09-21 已实际补修并验证，不再仅凭源码声明通过；正常步一次追加、reset 不追加。
- [x] B04：实际 100 步计数为每步一次 Lee compute、一次目标历史追加、四次全 batch 施力（reset 额外写选中行）。
- [ ] train／play 注册一致，checkpoint 能回放；导出策略与 PyTorch 结果一致。（注册一致；本轮 MLP checkpoint 保存重载通过，但 play.py 和新任务 JIT／ONNX 导出未执行，不能整体勾选）

不为文档修改运行 Isaac Sim。运行失败或依赖缺失应记录实际原因，不宣称已经通过。

> [!check] 2026-09-21 首次新任务实跑
> `list_envs.py` 与 `train.py` 均由用户在 `env_isaaclab` 中实跑成功。已勾选项均附实际证据；未勾选项一律保留，不因"整体跑通"而连带勾选。证据汇总见 [[07 工程上下文与接手约定#2026-09-21 实施与运行证据]]。

## 对照范围

首版仍只对照 PPO＋MLP 与 PPO＋测试图，验证工程集成。后续视觉阶段增加同一相机观测、帧时序、奖励、Critic、动作接口、训练步数和评估场景下的 CNN/MLP 基线，与冻结 FlyVis＋小型 MLP 比较；预训练来源、总参数量、可训练参数量、记忆机制和墙钟时间分别报告。尽量匹配可训练容量，不能用相同参数数掩盖预训练或计算预算差异。

有限联合训练通过后再做冻结／解冻消融，保持抽头、readout 和任务一致。若要声称连接拓扑导致行为变化，必须增加有向入／出度保持的随机拓扑消融，并记录节点角色、符号／类型约束、连通性和重训预算；随意打乱预训练网络后性能下降不能单独证明拓扑优越。

先报告碰撞率、完成率、通过时间及行为差异；“新动作”需事先定义可观察的轨迹／动作指标并在未见场景、多种子中复现。没有控制视觉预训练、记忆和 readout 容量时，只能称视觉模型与读出的集成结果；不能从有限实验声称全连接网络不可能产生该行为。

## 改动记录

- **源码位置**：任务注册、训练／回放入口及各模块页所列源码。
- **当前行为**：工程清理、包名迁移、局部 reset 修复和 CUDA 采样优化已实施；图 Actor 尚未实现。
- **计划操作**：依照阶段表实施，并按单项验收记录证据。
- **改后行为**：得到可运行最小基座，后续升级另行展开。
- **关联影响**：[[02 策略网络与 PPO 接入]] 与 [[03 观测动作与控制接口/00 接口改动总览]]。
- **验收条件**：阶段 1～5 有可复查证据，未完成项继续保持未实施／未验证。
- **审查状态**：首版清理与采样优化已实施并有专项证据；图 Actor、play／导出和默认规模仍未验收。

## 后续视觉 MVP 与真实图顺序

以下为分阶段计划；F01 独立探针及读出诊断已通过，正式相机闭环与其余阶段未验收；不改变首版阶段 0～6 的历史证据。首轮以单目视觉避障为目标，嗅觉、全脑仿真和硬件部署后置。

| 阶段 | 操作 | 必须留存的证据／门槛 |
|---|---|---|
| F0（F01） | FlyVis 固定版本、许可、安装、API 与权重；固定视频前向 | 权重来源／校验值、输入布局、实际节点及可训练参数清单、输出抽头、状态；Windows/Isaac Lab 同进程兼容性。**2026-09-23：API 与输入布局已静态读码闭合**（见 [[04 后续升级/01 FlyVis 与相机输入#2026-09-23 上游实现静态核查]]），后续本日 F01 已完成权重校验、安装与同进程探针，代码 MIT 已核实；权重单独许可尚未找到，完整结果见本页 F01 证据 |
| F1 | 相机-only 避障任务、固定几何适配与信息审计 | 相机外参、方向／亮度尺度、帧时序；目标信息清除；独立场景、奖励和评估配置。相机 FOV 决定每列张角（FOV／31），须显式登记选值；障碍与背景必须有亮度差（模型为灰度输入） |
| F2 | 冻结 FlyVis＋小型三轴 MLP＋PPO | FlyVis 参数不变、readout 更新；B×3 分布、速度／Lee、局部 reset、短闭环和 checkpoint 回放 |
| F3 | 同输入 CNN/MLP 基线 | 同任务、预算和种子；参数／预训练／时序差异记录；碰撞、完成和耗时指标 |
| F4（可选） | 有限共享参数联合训练及冻结／解冻比较 | 序列状态、截断与梯度验证；独立资源测量；不满足条件就保留 F2 |
| M1/M2 | 第二路线 MaleCNS 数据、连续值图和显式 adapter | 下方 E1～E6；FlyVis-only 不以这些项为前置 |

### 视觉任务的信息与动作边界

Actor 只接单目图像及视觉模型的时间活动，不接目标位置、跟踪向量、障碍真值／深度传感器或目标导出的 heading；低层 Lee 保留必需的姿态／速度反馈。环境碰撞真值仅用于奖励、终止和评估；Critic 不再沿用旧目标跟踪组，各对照共享同一新配置。

视觉阶段速度采用自身 yaw 对齐的水平坐标系（+X 前、+Y 左、+Z 上），偏航默认保持 episode 初始 yaw，关闭 face_target。reset 朝向、障碍和出生位置独立于隐藏目标；场景、奖励权重和评估条件在 F1 实施前固定并供所有对照共用。

### 资源、状态与验收场景

- [ ] F0：固定视频重复／分段推理一致；单环境与小批量对应；上下左右映射与亮度范围正确。方向正确性用**平移等变性**验证（周期平铺带来，需排除边界列并记录余量）并实测各方向选择性亚型的偏好方向；不得用「翻转输入→输出镜像」作为断言（wiring 不具反射／旋转对称性，T4a–d／T5a–d 按四个基本方向分亚型）。几何正确性另可在渲染阶段单独验证：已知方位亮条 → 核对受激列的 `(u, v)`。
- [ ] F0：确认 `BoxEye` 未触发 `min_frame_size` 隐式重采样（相机分辨率一次定好并核对），且 stride 优化后采样位置与原实现逐值一致。
- [ ] F1：Actor、速度坐标、偏航和 reset 均无目标真值路径。
- [ ] F2：局部 reset 不改变其他环境视觉状态；readout 三轴有有效梯度；冻结参数保持不变。灰屏稳态为固定向量，应开局算一次后按 env_ids 索引拷贝，不逐 reset 重跑预刺激；`Stimulus` 可经 `AVAILABLE_STIMULI` 注册自定义子类以管住 buffer 生命周期，避免 `zero()` 每步重分配（B=1024 时单 buffer 约 187 MB）。
- [ ] F2：冻结阶段将 FlyVis 前向包入 `no_grad`，确认梯度只经 readout；据此分别测量冻结与解冻的显存和耗时（参数少不代表激活与计算图开销小）。
- [ ] F2：从 B=1 开始测峰值显存和吞吐，再递增小批量；记录图像尺寸、视觉子步、序列长度及采样／PPO 更新时间。
- [ ] F2/F4：在扩规模前登记可接受的训练迭代耗时和显存余量；完整采样＋更新周期无 OOM、无非有限值且满足预算才扩规模。
- [ ] F2：checkpoint 重载、状态初始化、确定性回放和导出与 PyTorch 一致。
- [ ] F3/F4：基线及冻结／解冻实验使用相同信息、readout 设置和评估条件。
- [ ] 拓扑归因：执行保度随机拓扑消融并报告连通性、重训和预训练差异，未执行不声称拓扑产生独有动作。

无相机 2048／4096 环境的历史结果不支持相机规模。失败先降低批量、图像或序列预算；必要时离线缓存活动做接口检查，但不把离线拟合称为 PPO 闭环。F4 不通过不触发全脑重写，MaleCNS 是独立后置选择。

## 后续 FlyDrones 扩展验收（不阻塞首版）

以下 E1～E6 属于视觉 MVP 与基线之后的 M1/M2 第二路线；F4 可选，不作为相机闭环的前置。

| 顺序 | 验收对象 | 必须留存的证据 |
|---|---|---|
| E1 | 固定上游源码与数据来源 | 本地 commit、代码／数据许可、实际数据文件版本与校验值 |
| E2 | 独立数据加载和分组 | 不创建 Brain；group 数量、类型／侧别、roles、空必需群失败结果 |
| E3 | 子图和索引 | pre→post 方向、body_id 重映射、往返保存、路径与循环检查 |
| E4 | PyTorch 计算 | 有效边权更新；同图／同输入可复现；不调用 NumPy LIF 作为可微策略 |
| E5 | 第二路线跨模型／运动映射 | FlyVis→MaleCNS 显式对应表；三维学习读出；无人工 cruise／escape 控制捷径 |
| E6 | 集成与回放 | 状态 reset、PPO 序列、checkpoint 版本、B×3 动作及导出一致 |

上游 tests/test_malecns_loader.py 使用真实列名的微型假数据；tests/test_brain.py 大量使用 MiniFly。可以参考或复用断言，但不能把这些测试的存在／通过当成完整 MaleCNS 数据或无人机控制验证。本次未运行这些测试。

- [ ] sensorimotor_core 的 meta.roles 已显式设置。
- [ ] 稀疏矩阵按 W[post,pre] 转换，群内节点仍对应正确 body_id。
- [ ] max_neurons 截断后复查可达性；必留输入输出超预算时明确报错或调整预算。
- [ ] 签名／初始化规则与训练权重可区分，未知递质不被隐藏成确定兴奋性。
- [ ] loader 与图配置漂移导致 checkpoint 不兼容时明确拒绝加载。
- [ ] 视觉-only／嗅觉搜索同步移除 heading、偏航和 reset 的目标真值依赖。

详见 [[04 后续升级/06 FlyDrones 连接组数据适配]]、[[04 后续升级/07 感觉运动节点映射与可训练读出]] 与 [[06 本地代码库与复用清单]]。

## 审查意见模板

> [!note] 可复制到对应模块页
> 条目／改动 ID：
> 审查意见：
> 影响的接口／文件：
> 需要调整的计划：
> 实施与验证证据：
> 状态：未实施／实施中／已验证（附证据）

## 本次文档交付验证

此处仅由文档生成后的静态检查回填；与上面的工程验收分开记录。

## 已存在源码入口

以下是核查时实际存在的文件；正文中的“拟新增”文件不在此表冒充现状。

| 源码位置（相对代码仓库） | 依据 |
|---|---|
| [scripts/rsl_rl/list_envs.py](<D:/zerozero_code/fruit-dronefly/scripts/rsl_rl/list_envs.py>) | 2026-09-21 实跑通过，仅输出 1 个任务 |
| [scripts/rsl_rl/train.py](<D:/zerozero_code/fruit-dronefly/scripts/rsl_rl/train.py>) | 2026-09-21 实跑通过，60 迭代 |
| [scripts/rsl_rl/play.py](<D:/zerozero_code/fruit-dronefly/scripts/rsl_rl/play.py>) | 已存在；**未实跑** |
| [source/fruit_dronefly/setup.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/setup.py>) | 已迁移；`name="fruit-dronefly"` |
| logs/rsl_rl/fruit_dronefly_v0/2026-09-21_13-34-39/ | 运行产物：tfevents、`model_0.pt`、`params/`、`git/` |


### 初版静态检查记录（扩展前历史）

- 已检查 15 篇笔记的 YAML 状态、内部链接和代码引用路径。
- 72 处内部链接、50 处本地源码链接均可解析。
- Canvas JSON 有效；18 个节点、21 条边，覆盖全部笔记。
- 写入内容与生成清单一致；源码目录与现有 .obsidian 配置的写入前后内容哈希一致。
- 尚未通过 Obsidian GUI 核实 Mermaid／Canvas 的最终显示效果。
- 该段保留为扩展前历史记录；本轮新增运行证据不覆盖 FlyDrones、真实连接组或图 Actor。


### FlyDrones 扩展版静态检查（接手补全前历史）

- 当前 18 篇笔记；118 处内部链接、85 处本地文件链接通过静态检查。
- Canvas：21 个节点、31 条边，覆盖全部笔记。
- 本轮依据本地 FlyDrones 3e269346b3882c291d2a977bc2c2c6a9c9213c21 更新，不代表运行验证。
- 未安装外部依赖、运行 Isaac Lab／PPO／LIF、下载真实连接组或调用硬件。
- 当时未通过 Obsidian GUI 核实最终布局；这是扩展前历史状态，当前实施状态以上方验收表为准。

## 独立接手文档验收

- 独立阅读问题与答案入口集中在 [[07 工程上下文与接手约定#独立阅读自检]]；覆盖目标、目录、当前状态、首版范围、接口、复用边界、下一步及证据。
- 两处 README／AGENTS 必须指向同一知识库，不复制第二套实施方案。
- 检查内部页与标题链接、本地源码路径、入口 Markdown 链接、Canvas JSON／节点引用和文档覆盖。
- 外链统一标明来源与未联网核查状态；本轮不据此声称外部 API 兼容。
- 对照接口页核查 19 维观测、3 维动作、5×tanh、目标连线 heading、25 Hz 控制／100 Hz 物理时序；不能把声明的依赖版本当安装版本。
- 本项约束针对当时的独立接手文档交付；本轮用户另行授权 CUDA 源码优化及测试。此次 Obsidian 回填仅改文档，参考仓库与 .obsidian 保持不变。

该句属于历史文档生成记录；当前运行证据见上方验收表和 CUDA 采样专项，文档回填本身未启动 Obsidian GUI。

### 独立接手版静态检查记录

- 本轮文档检查：21 篇知识库 Markdown、2 篇源码导航文档；Canvas 24 个节点、36 条边。
- 内部页面与标题、Markdown 本地链接及 Canvas 引用均通过静态校验；独立阅读问题均有库内答案入口。
- 接口摘要与对应模块交叉核查；已区分源码依赖声明、实际安装事实和运行结果。
- 外部链接未联网访问；未运行新会话 agent 或 Obsidian GUI，不将结构检查等同实际接手测试。
- 该段为历史接手版记录；截至 2026-09-22，源码清理、局部 reset 与 CUDA 采样优化已实施并完成专项验证。

## 原 v0 兼容性实测补充

2026-09-20 在 env_isaaclab 显式选择新仓库中的原 v0 源码，完成 2 环境／100 步、2 次 PPO、保存重载、JIT 数值一致与 ONNX checker，子进程返回 0。详细覆写、限制及库内证据见 [[08 运行环境兼容性与版本方案]]。本页上方文档交付记录属于各次交付的历史记录；新的测试不回填为新任务或测试图验收已完成。

2026-09-21 局部 reset 补修验收见 [[03 观测动作与控制接口/04 Lee 控制器与执行时序#2026-09-21 局部 reset 修复与专项验收]]；之后本轮 CUDA 优化复跑该专项并补做 3 次 PPO smoke，见下方 P12。play／导出及完整首版验收仍未通过。

## 日常自动化回归

测试清单、执行建议和结果判定集中维护于 [[09 自动化测试与回归检查]]。此页维护工程验收，新指南维护日常运行方式；按本次变更实际执行并记录结果。

## CUDA 采样优化专项（P12）

- [x] reset 查表：CPU/CUDA 4 项通过。
- [x] 朝向计算：CPU/CUDA 12 项通过，固定形状路径不产生动态 nonzero。
- [x] 轨迹分组：CPU/CUDA 通过，覆盖七种轨迹、组交换、消失／重建、对象复用及状态／随机数一致性。
- [x] 真实局部 reset：4 环境、100 步物理回归通过。
- [x] PPO smoke：3 次更新，loss／权重有限，Actor 参数变化，checkpoint 重载输出一致。
- [x] 性能 A/B：2048 环境 ordinary／partial reset／curriculum reset 均值下降 9.9%／19.9%／34.0%；独立分组函数 1／64 个环境变化由 86.891／135.522 ms 降至 1.394／30.942 ms。

证据：[CUDA 优化验证摘要](<D:/zerozero_code/fruit-dronefly/logs/validation/cuda_optimization_20260921/summary.md>)；限制是单次有界 A/B，不代表长期训练吞吐、收敛、默认 4096 环境或完整 CUDA kernel timeline。

脚本清单与运行命令见 [[09 自动化测试与回归检查]]；历史验证基线为 f7f085d 加工作树，11 个受测文件的 SHA-256 与 metadata.json 一致；随后 CUDA 优化已提交 f473d7b。


## 2026-09-23 P03：理想执行模型验证

用户将电机/旋翼可达性暂缓，当前按理想机体推力/力矩接口验收。已修正 Lee 陀螺补偿为力矩空间的 ω×Jω，并在零合力和推力方向与 heading 共线时回退到合法旋转矩阵；保持原控制增益、速度/加速度/总推力约束和 25 Hz/100 Hz 时序。CPU/CUDA 各 512 状态解析检查、12 场景修改前后闭环对照（24 环境、12 s）及原 4 环境局部 reset 回归通过；两个进程退出码 0。末段平均速度向量误差最大约 0.0001852 m/s，进入并保持 0.1 m/s 误差范围最慢约 1.44 s。修改前也通过这些速度场景，不能把此次公式修正宣称为显著改善常规速度跟踪。组合速度反向时实际单轴峰值约 5.49 m/s；速度指令限幅不是实际速度硬限幅。功能改动已提交 `acfdfcf`（2026-09-23 复核）；历史受测状态、详细覆写、限制与日志见[验证证据](<D:/zerozero_code/fruit-dronefly/logs/validation/p03_control_20260923/summary.md>)。

复现：`python scripts/tests/check_lee_control.py`，随后 `python scripts/tests/check_partial_reset.py`。电机能力、PPO 重训和任意状态全局稳定性不在本次通过范围。

## 2026-09-23 F01：冻结视觉独立探针与读出验证

本轮完成独立固定视频探针、冻结模型与最小读出、真实 RSL-RL PPO 接口诊断。基线为 `acfdfcfc17cec03812ec8c4f21c96d966b6fa920` 加未提交工作树；[summary.json](<D:/zerozero_code/fruit-dronefly/logs/validation/flyvis_f01/summary.json>) 保存 12 个受测源码／配置／测试文件 SHA-256，本次回填重新核对一致。摘要中“按用户要求未修改 Obsidian”是实施当时事实；此次用户另行授权回填，不改写旧报告。

| 验证 | 结果与边界 |
|---|---|
| 读出与 Windows | 读出 5 项 CPU/CUDA 测试及 datamate HDF5 回归 1 项通过 |
| 固定视频 | 官方预训练权重；403×403 灰度；BoxEye 对照直接 13×13 窗均值，无隐式缩放；原 stride 未优化 |
| 状态 | 连续/分段、batch/单环境、局部/重复/空/全量 reset、未重置行下一步一致；冻结参数无梯度且不变 |
| PPO | 4 环境×16 步×3 次更新；真实活动输入，卷积/MLP 更新、三轴梯度、有限 loss 与正标准差 |
| 保存与导出 | Actor/Critic/优化器 checkpoint 重载；含前端的 JIT/ONNX 数值一致，动态 batch 1/4 |
| Isaac Lab 同进程 | 100 步通过、退出码 0；物理动作全零，视频独立于仿真；唯一任务仍为 IsaacLab-FruitDronefly-v0 |
| 原物理回归 | 4 环境100步通过；七类轨迹、历史/延迟/控制器/composer、课程分组、25步频繁reset对照、下一物理步一致；每步一次历史/Lee与四次施力 |

诊断奖励为 `-sum(action**2)`，不是飞行任务奖励；本轮不证明收敛或避障成功。导出只覆盖活动→动作均值，未导出 FlyVis 动态网络。完整任务 play、首版测试图、相机 F1/F2、方向调谐/平移实测、有限解冻、默认4096规模和长期训练仍待验收，原阶段复选框不整体勾选。

RTX 5060 Laptop 8 GB，固定视频每批64帧：B=4 视觉步均值12.6379 ms/batch、读出0.7761 ms/batch、推理峰值 Torch allocated 310,115,328 bytes（295.75 MiB）；B=1 为8.66763 ms、190,086,656 bytes（181.28 MiB）。三次 PPO 更新约0.313/0.160/0.215 s。这里不含完整相机渲染训练预算，也不是整卡显存峰值。

详细证据：[视频与真实活动 PPO](<D:/zerozero_code/fruit-dronefly/logs/validation/flyvis_f01/video_final/results.json>)、[PPO](<D:/zerozero_code/fruit-dronefly/logs/validation/flyvis_f01/video_final/ppo/results.json>)、[同进程](<D:/zerozero_code/fruit-dronefly/logs/validation/flyvis_f01/isaac/results.json>)、[物理回归](<D:/zerozero_code/fruit-dronefly/logs/validation/flyvis_f01/partial_reset.log>)。结构见 [[04 后续升级/07 感觉运动节点映射与可训练读出]]，环境见 [[08 运行环境兼容性与版本方案]]，复现命令见 [[09 自动化测试与回归检查]]。
