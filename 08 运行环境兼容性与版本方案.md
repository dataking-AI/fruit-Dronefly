---
tags:
  - fruit-dronefly
  - 环境兼容性
类型: 实测审查
工程状态: 首版清理与 CUDA 采样优化已实施，图 Actor 未实施
文档审查状态: 已核查并记录实测证据
代码实施状态: 新任务清理、局部 reset 与采样优化已实施；连接组策略未实施
运行验证状态: 局部 reset、CUDA 优化与 PPO smoke 专项通过；play／导出／4096 规模未验证
核查日期: 2026-09-22
---

# 运行环境兼容性与版本方案

[[README|阅读入口]] · [[07 工程上下文与接手约定|工程上下文]] · [[06 本地代码库与复用清单|仓库版本]] · [[02 策略网络与 PPO 接入|策略适配]] · [[01 仓库清理与任务收敛#包名统一决策|包名复审]]

## 结论与验证边界

**env_isaaclab 的 Python、GPU、Isaac Sim、Isaac Lab 和 RSL-RL 能运行当前复制仓库的 v0 环境及 PPO，可作为首版实施基座，无需为测试图策略降级 Python 或仿真框架。** 本机已完成 2 个环境的 100 步检查、2 次 PPO 更新、checkpoint 保存重载、JIT 和 ONNX 导出。进程退出码为 0。

上述是 2026-09-20 原 v0 的历史探针，不是新任务验收；新任务此后已实施并有本轮专项证据，连接组 Actor 仍未实现，也不等于长时间训练、无人机控制性能或后续 FlyVis 兼容性验证。RSL-RL 5.0.1 使用分开的 actor/critic 模型，方案需据此接入，不能照搬旧版自定义 ActorCritic 类。

## 实际解释器与版本

下表保留 2026-09-20 原 v0 探针的版本与安装记录。迁移后新增 fruit-dronefly 0.1.0，fruit_dronefly 解析到新源码树；本轮 CUDA 采样验证沿用相同核心栈。

解释器：`D:\software\miniconda3\envs\env_isaaclab\python.exe`。直接使用此解释器复核，避免查询 base 环境。

| 组件 | 实测版本／位置 | 解释 |
|---|---|---|
| Python | 3.11.16，Windows AMD64 | Isaac Sim 5.1 使用 Python 3.11；本次实际启动通过 |
| Isaac Sim | 5.1.0.0 | env_isaaclab 内 pip 分发，含 core/kernel 等组件 |
| Isaac Lab 源码 | D:\zerozero_code\IsaacLab；b0542fe2d45bf91c4e1d9ef6952b9c709c80b4e8 | editable 实际加载位置；VERSION 文件为 2.3.2，但 HEAD 不是 v2.3.2 tag |
| Isaac Lab 发行包 | isaaclab 0.54.4；isaaclab_rl 0.5.2 | 子包版本号，不等同仓库发布号；锁定源码 commit，不能据此 pip install isaaclab==2.3.2 |
| PyTorch / CUDA | 2.7.0+cu128 | CUDA 可用，包含 sm_120，GPU 矩阵计算和反向传播通过 |
| torchvision | 0.22.0+cu128 | 与当前 torch 同系列 |
| RSL-RL | rsl-rl-lib 5.0.1 | 已安装；包名与 Python 导入 rsl_rl 不同 |
| Gymnasium | 1.2.1 | 与本地 IsaacLab setup.py 一致 |
| TensorDict | 0.14.2 | 当前模型及 PPO 运行通过 |
| 导出依赖 | onnx 1.22.0；onnxscript 0.7.2 | 本次 ONNX 导出及 checker 通过；不代表 ONNX Runtime 推理已测 |
| GPU / 驱动 | RTX 5060 Laptop 8 GB；592.19 | 仅验证 2 个环境；大 batch／相机显存预算另测 |
| DroneFollow 安装 | 0.1.0，editable 指向 D:\zerozero_code\dronefollow\source\DroneFollow | 默认会载入原项目；本次进程显式优先加入新源码路径，并验证 __file__ |

最初用“导入名映射”查询发行包曾误报缺包，已通过 importlib.metadata 的发行包信息和实际导入纠正。rsl-rl-lib、isaaclab_rl、isaacsim-core 均已安装。不能按模块名是否出现在 packages_distributions 键中判断发行包不存在。

## 实测结果及复现条件

本节为 2026-09-20 原 v0 历史记录，其中五个任务注册、2 环境和导出结果不能当作当前新任务的测试。2026-09-21 后的新任务及 CUDA 优化证据见本页末尾与 [[05 实施顺序与验收]]。

| 检查 | 结果 | 限制 |
|---|---|---|
| CUDA 与 autograd | 通过 | 普通张量运算，不是真实连接组性能测试 |
| RSL-RL 模型与类解析 | 通过；action=(8,3)，value=(8,1)，模块:类路径可解析 | MLPModel；自定义图类尚未实现 |
| Isaac Sim AppLauncher | 无界面启动成功 | 先启动 app，再导入任务；普通 Python 直接 import 任务报 pxr 缺失不是版本不支持 |
| 工程加载 | __file__ 位于 fruit-dronefly；旧五个 ID 正常注册 | 本次仅调整进程 sys.path，未覆盖环境的原 editable 安装 |
| 旧 PPO 配置转换 | 通过 | 转为两个 MLPModel；GaussianDistribution(init_std=0.5,std_type=scalar) |
| 动作 wrapper | clip_actions=None | 当前原 v0 配置，无额外 wrapper 裁剪；未来 CLI／新配置需重新核对 |
| 环境 | 2 个环境，Actor/Critic 观测均 (2,19)，动作 3 维；100 步无非有限观测／奖励 | 不等于全部 reset、坐标和控制参数回归通过 |
| PPO | 2 iterations 完成；数值可用 | mini-batches 改为 2 配合小 batch；不评价收敛或飞行品质 |
| checkpoint | 保存、重载、确定性动作 (2,3) 有限 | 同一 runner 重载；未执行完整 play.py/HUD 路径 |
| JIT | 导出成功；8 组输入与原模型最大绝对误差 0 | 当前 MLP，不涵盖未来图结构 |
| ONNX | 导出与 onnx.checker 通过 | 尚未做 ONNX Runtime 数值对比 |
| 退出 | env.close 已调用，子进程返回 0 | 用 subprocess 精确记录，避免 PowerShell 原生命令 stderr 干扰退出判断 |

测试覆写仅用于兼容性探针：2 个并行环境、STAGE=0、移除远程地形装饰材质与天空 HDR、PPO mini-batches=2。无人机 USD、地形几何、观测、动作、Lee、奖励和终止仍使用当前 v0。没有安装、卸载或升级任何包，没有改功能源码；测试产物在临时目录，摘要和日志已复制到本库。

USD 中若干部件出现惯量近似提示，测试仍完成；不能据此声称真实惯量已标定。8 GB GPU 可运行此次小规模无相机检查，不能承诺原默认 4096 环境或 FlyVis＋相机的规模。

## 首选版本方案：锁定已实测核心栈

采用上表的 Python 3.11.16＋Isaac Sim 5.1.0.0＋IsaacLab commit b0542fe2d45bf91c4e1d9ef6952b9c709c80b4e8＋torch 2.7.0+cu128／torchvision 0.22.0+cu128＋rsl-rl-lib 5.0.1＋gymnasium 1.2.1。当前功能目标不需要 Python 3.12、Isaac Sim 6 或 SNN 依赖。

实施时先保存 conda/pip 清单、IsaacLab commit 与工作区差异；建议为 FruitDronefly 创建专用环境副本再调整依赖和安装项目，原 env_isaaclab 保留供原 DroneFollow 使用。conda clone 可能仍指向相同 editable 源码，必须显式核对导入路径；“克隆环境”不等于“复制源码”。

此方案是**实测核心链路基线，不是无冲突的完整 pip lockfile**。以下 pip check 问题需要单独处理，不应一次性升级所有包：

| 冲突／缺失 | 具体约束 | 处理方案与边界 |
|---|---|---|
| torchaudio 缺失 | isaacsim-core 要求 2.7.0 | 专用环境补 torchaudio 2.7.0，选与 torch 对应的 cu128 wheel；本轮未安装 |
| fastapi / starlette | Sim kernel 固定 fastapi 0.115.7，要求 starlette<0.46；当前 Lab 固定 starlette 0.49.1 | 约束无交集，不能用一个普通版本 pin 同时满足；无 Web 服务的训练主链已测可用，Web/直播不列为已验收。若要求 pip check 全通过，须另建测试过的框架依赖补丁或切换经完整求解的发行组合，不能称简单重装可解 |
| ipython / psutil | ipython 9.17.1 要求 psutil>=7，Sim 固定 5.9.8 | 专用最小训练环境不装非必需 IPython；保留 Sim 的 psutil 5.9.8，不为 notebook 升级控制环境依赖 |
| click | Sim 固定 8.1.7，当前 8.5.0；已装 wandb 要求 >=8.2 | 专用环境使用 TensorBoard，移除非必需 wandb 后固定 click 8.1.7；当前环境不直接改 |
| typing_extensions | Sim 固定 4.12.2，当前 4.16.0；onnx 1.22.0 要求 >=4.15 | 不能只降 typing_extensions；若要求元数据一致，应另测较早 ONNX 及其整套导出依赖，当前已实测导出组合先记录保留 |
| stable-baselines3 | 2.9.0 要求 torch>=2.8，当前 2.7.0 | 本任务只用 RSL-RL，专用最小环境不安装 SB3；不为无关算法升级 torch |
| wheel / packaging | wheel 0.47.0 要求 packaging>=24，Sim 固定 23.0，Lab RL 要求 <24 | 专用环境使用 wheel 0.45.1＋packaging 23.0 作为修复候选，先 dry-run 与 pip check；本轮未执行 |

以上为当前冲突的处理边界，不能把未执行的修复标记为已验证。特别是两组上游严格约束冲突意味着不存在保持全部当前发行包声明不变的“完美 pin”。不使用 --no-deps 将冲突隐藏成已解决。

## 备选版本方案：正式 tag 与旧版策略接口

仅当需要使用正式发行 tag 或保留旧 ActorCritic 插件方式时考虑：Python 3.11.16、Isaac Sim 5.1.0.0、IsaacLab **v2.3.2 / 37ddf626871758333d6ed89cf64ad702aef127d0**、torch 2.7.0+cu128、torchvision 0.22.0+cu128、gymnasium 1.2.1、**rsl-rl-lib 3.1.2**。3.1.2 来自该 tag 的 RL extra，不是当前 HEAD 的 5.0.1。

该备选的源码依赖已通过本地 git show 核对，**未安装、未运行，不能当作本机通过验证的环境**。同一 v2.3.2 tag 仍含 starlette 0.49.1 声明，因此它不是上述全部依赖冲突的自动解决方案。

迁移必须在独立源码 checkout 与专用 conda 环境中进行，同时将项目 train/play 的 RSL-RL 集成对齐该 tag：该版不导出当前代码引用的 handle_deprecated_rsl_rl_cfg / handle_deprecated_rsl_rl_checkpoint；不能只降 RSL-RL 或只 checkout tag 后直接运行当前脚本。使用 3.1.2 的 ActorCritic 协议另做适配，重新执行环境、PPO、checkpoint 与导出验收，不复用 5.0.1 checkpoint 假定兼容。

首选仍是已实测的当前核心栈；目前没有因为首版需求而切换到此备选的技术必要。

## 对策略实现方案的修正

- 采用 RSL-RL 5.0.1 的 `actor`／`critic` 两个模型配置；Actor 实现 MLPModel 的协议或继承其通用接口，替换内部特征到均值的网络为 masked graph，Critic 保留 MLPModel。
- 明确 `obs_groups={"actor":["policy"],"critic":["critic"]}`，使用 TensorDict 观测；环境接口不变。
- Actor 配置使用 `class_name="fruit_dronefly.policies.sparse_actor:SparseGraphModel"`。resolve_callable 原生支持模块:类，不需要修改 PPO 或维护额外 monkey patch factory。该类为拟新增，包路径已确定为 fruit_dronefly。
- 高斯分布保持 init_std=0.5；首版保持现有 scalar 标准差方式，与 MLP 一致，并检验标准差合法。图 Actor 不再额外 tanh。
- 实现 forward、distribution/log-prob/entropy/KL、normalization、无状态 reset/get_hidden_state 及 as_jit/as_onnx 协议；优先复用基类已有方法，图特有部分必须实测。
- 新模型使用原生 5.0.1 checkpoint 布局；图 mask、映射、模型类和配置须可恢复。旧 MLP 的兼容转换器不会自动转换新图策略。

## 包名结论

暂留 DroneFollow 的收益仅是减少首版 import、打包和注册路径改动，没有运行性能或算法收益。本机已有同名安装且指向原工程，副作用已经存在。用户已确认趁新模型和 checkpoint 尚未产生，在清理阶段统一：仓库／发行名 `fruit-dronefly`，Python 包 `fruit_dronefly`，环境 ID `IsaacLab-FruitDronefly-v0`。连字符不能用于 Python import，三者按各自规范命名即可。

这是已实施的命名决策，**源码与安装改名已完成，清理结果提交于 f7f085d**；原先“暂留包名”仅作为历史方案，不再作为默认实施基线。迁移清单和验收见 [[01 仓库清理与任务收敛#包名统一决策]]。

## 证据与核查入口

- [结构化测试结果](<附件/2026-09-20 环境兼容性核查/result.json>)、[进程退出码](<附件/2026-09-20 环境兼容性核查/exit.json>)、[运行日志](<附件/2026-09-20 环境兼容性核查/probe.log>)、[pip check 输出](<附件/2026-09-20 环境兼容性核查/pip-check.txt>)。
- [探针源码快照](<附件/2026-09-20 环境兼容性核查/probe.py.txt>)、[启动器快照](<附件/2026-09-20 环境兼容性核查/launcher.py.txt>)：供复核条件，不是已纳入工程的测试套件。
- checkpoint/JIT/ONNX 位于 C:\Users\zerozero\AppData\Local\Temp\fruit_env_probe_ppo，未复制模型进知识库；临时模型可能被系统清理，以上日志与结果已在库内持久保存。
- [已安装 PPO 构造接口](<D:/software/miniconda3/envs/env_isaaclab/Lib/site-packages/rsl_rl/algorithms/ppo.py>)、[MLPModel 协议](<D:/software/miniconda3/envs/env_isaaclab/Lib/site-packages/rsl_rl/models/mlp_model.py>)、[模块解析](<D:/software/miniconda3/envs/env_isaaclab/Lib/site-packages/rsl_rl/utils/utils.py>)。
- [原工程 editable 安装记录](<D:/software/miniconda3/envs/env_isaaclab/Lib/site-packages/dronefollow-0.1.0.dist-info/direct_url.json>)、[IsaacLab 当前 RL extra](<D:/zerozero_code/IsaacLab/source/isaaclab_rl/setup.py>)。
- [IsaacLab 正式 v2.3.2 依赖源码](https://github.com/isaac-sim/IsaacLab/blob/37ddf626871758333d6ed89cf64ad702aef127d0/source/isaaclab_rl/setup.py)：来源由本地 git 对象核查，本轮未联网访问。

## 工程审查记录

- **源码位置**：上述本地框架、已安装 RSL-RL 和原 v0；图 Actor 仍为拟新增。
- **当前行为**：新源码已收敛并可运行 MLP PPO；局部 reset、CUDA 采样路径和 PPO smoke 已专项验证，依赖检查仍有冲突。
- **计划操作**：保持已实测核心栈，先完成本轮 CUDA 优化提交、play／导出和默认规模验收，再按 5.0.1 原生模型接口实现图策略。
- **改后行为**：新任务与图策略继承已验证的环境能力，避免旧包串用和错误版本接口。
- **关联影响**：策略配置、checkpoint／导出、安装路径、仓库索引和阶段 0 记录。
- **验收条件**：新任务仍须完整执行原验收；新增图权梯度、mask、导出与保存恢复检查；不能沿用原 v0 证据代替。
- **审查状态**：清理、局部 reset 和 CUDA 采样优化已实施并完成专项验证；图 Actor、长期训练、依赖全一致性和后续模块未验证。

## 2026-09-22 CUDA 采样专项

本轮在 env_isaaclab、Torch 2.7.0+cu128、RTX 5060 Laptop 8 GB、Isaac Sim 5.1 下完成 CUDA 采样优化和有界 A/B。2048 环境均值：ordinary 119.24→107.40 ms、partial reset 205.62→164.75 ms、curriculum reset 340.93→224.87 ms；独立分组 1／64 个环境变化中位数 86.891／135.522→1.394／30.942 ms。

这只代表本机本配置的一次局部对照；普通场景中位数基本不变，未测试默认 4096 环境、远程材质、长期训练收敛或完整 CUDA kernel timeline。详见 [CUDA 优化验证摘要](<D:/zerozero_code/fruit-dronefly/logs/validation/cuda_optimization_20260921/summary.md>)。
