---
tags:
  - fruit-dronefly
  - 自动化测试
类型: 测试指南
文档审查状态: 已核对脚本、日志和源码哈希
代码实施状态: 五个回归脚本和一个基准脚本已存在；统一 runner 与 CI 未实现
运行验证状态: CPU/CUDA 专项、物理回归、PPO smoke 及有界 A/B 通过
核查日期: 2026-09-22
---

# 自动化测试与回归检查

[[README|阅读入口]] · [[05 实施顺序与验收|工程验收]] · [[07 工程上下文与接手约定|接手状态]]

## 用途与当前实现

测试位于源码仓库 scripts/tests/，断言负责判定覆盖行为，仍需手动逐个启动。朝向测试支持 unittest discover；尚未配置统一调度、Git hook 或 CI。可复制的运行说明也见 [scripts/tests/README.md](<D:/zerozero_code/fruit-dronefly/scripts/tests/README.md>)。

| 脚本 | 覆盖范围 | 运行要求 |
|---|---|---|
| [test_reset_lookup.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/test_reset_lookup.py>) | 两种地形表布局、轨迹起点与地形原点区别、课程等级、空／重复／乱序／全量索引、reset 几何与未选环境隔离 | env_isaaclab；启动 Sim 供导入，不建物理场景；CPU/CUDA |
| [test_trajectory_orientation.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/test_trajectory_orientation.py>) | 零速度、阈值、各象限、NaN/Inf 与原行为一致；七种轨迹、选中行隔离；朝向计算不产生 aten::nonzero | 无需启动 Sim；CPU/CUDA 张量测试 |
| [check_trajectory_grouping.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/check_trajectory_grouping.py>) | 七种轨迹增量分组、交换、消失／重建、对象复用、未选环境隔离；连续状态／输出和随机数序列与完整重分组参考一致 | env_isaaclab；启动 Sim 供导入；CPU/CUDA |
| [check_partial_reset.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/check_partial_reset.py>) | 4 环境、100 步真实物理回归；action_lag=2、approach、局部／重复／空／slice／全量 reset、手动 level 切换、目标历史、真实 composer、下一物理步和调用计数 | env_isaaclab；GPU 物理场景，本地材质 |
| [check_critic_observations.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/check_critic_observations.py>) | 实际配置和函数，Actor/Critic B×19、真值／噪声／延迟隔离、heading 解析值及动作历史 | env_isaaclab；启动 Sim 供导入，不建物理场景 |
| [benchmark_cuda_sampling.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/benchmark_cuda_sampling.py>) | 三种采样场景计时；可选 3 次 PPO 更新、loss／权重有限、Actor 参数变化、checkpoint 重载确定性输出一致 | env_isaaclab；GPU 物理场景，本地材质 |

## 每次修改的执行建议

| 修改范围 | 建议检查 |
|---|---|
| 文档、注释或链接 | 内容、链接和差异检查，不启动 Sim |
| reset 轨迹起点或地形表 | reset 查表＋局部 reset 物理回归 |
| 轨迹朝向或分组刷新 | 朝向＋分组＋局部 reset 物理回归 |
| 观测、目标读取、噪声／延迟 | Critic 专项；涉及 reset／命令时加局部 reset |
| 动作、控制器和施力 | 局部 reset；涉及 heading 或动作历史时加 Critic；补足改动直接影响的断言 |
| CUDA 采样优化 | 五个回归脚本；同配置 A/B 和 PPO smoke |
| 网络、奖励或训练／回放／导出 | 对应接口专项及训练／回放／导出验证；采样基准不能替代 |

测试失败先定位原因；有意改变接口时同步修改规格和测试预期，不以删除断言获得通过。纯 CPU 通过不能替代 CUDA 验收。

## 运行命令

从源码根目录、env_isaaclab 运行。每条命令结束后核对退出码和完成标记；Isaac Sim 脚本必须逐个启动，不并行开多个实例。

~~~powershell
conda activate env_isaaclab
cd D:\zerozero_code\fruit-dronefly
python -m compileall -q source/fruit_dronefly scripts
git -c core.whitespace=cr-at-eol diff --check
git -c core.whitespace=cr-at-eol diff --cached --check
python -m unittest discover -s scripts/tests -p test_trajectory_orientation.py -v
python -u scripts/tests/test_reset_lookup.py
python -u scripts/tests/check_trajectory_grouping.py
python -u scripts/tests/check_partial_reset.py
python -u scripts/tests/check_critic_observations.py
~~~

reset 查表预期 4 项 OK（每项含 CPU/CUDA）；朝向预期 12 项 OK（6 CPU＋6 CUDA）；分组应包含两设备各四组 PASS 及最终完成标记；局部 reset 应有六组 PASS，末组为 100 steps；Critic 应有最终 actor/critic (2, 19) PASS。CUDA 不可用时会跳过 GPU 子项，要明确记录。

退出码 0 本身不足以判定成功：同时检查 Traceback、断言失败、初始化错误及预期 OK/PASS。Sim 关闭过程可能掩盖脚本中途退出。不要用关闭断言的 python -O。

## 采样基准与 PPO smoke

~~~powershell
python scripts/tests/benchmark_cuda_sampling.py --num-envs 2048 --steps 30 --warmup 10 --output-dir logs/validation/cuda_sampling/current
python scripts/tests/benchmark_cuda_sampling.py --num-envs 64 --steps 1 --warmup 1 --ppo-smoke --output-dir logs/validation/cuda_sampling/ppo_smoke
~~~

第一条复现本轮测量规模；第二条单独验证 3 次 PPO 更新，1 步采样数据不作为性能证据。脚本使用 headless、无录像、本地材质、零速度策略动作、默认 3×14 地形网格。关闭自动课程，分别测量 ordinary、每五步重置四分之一环境、每五步改变难度并重置四分之一环境；ordinary 仍允许自然终止/reset。采样在 no_grad 下进行，计时边界显式同步 CUDA，有限值检查在计时结束后执行。

A/B 使用相同参数、明确的优化前源码基线。本轮基线是 f7f085d，将原 commands.py、events.py、trajectory.py 保存为 commands_before.py、events_before.py、trajectory_before.py，再向脚本传 --baseline-dir。它只在内存替换本轮三条优化路径，不回滚工作树。格式见 [测试说明](<D:/zerozero_code/fruit-dronefly/scripts/tests/README.md>)；仅使用可信本地基线。计时不设自动通过阈值，机器负载、GPU 频率及预热会影响结果。

## 日志和已有证据

本轮证据：[summary.md](<D:/zerozero_code/fruit-dronefly/logs/validation/cuda_optimization_20260921/summary.md>)、[metadata.json](<D:/zerozero_code/fruit-dronefly/logs/validation/cuda_optimization_20260921/metadata.json>)。基线 f7f085d 加未提交工作树，元数据记录的 11 个源码／测试文件 SHA-256 与 2026-09-22 回填时一致。日志位于 Git 忽略的 logs/validation/；这些是本机产物，测试代码应随源码保存。

| 验证 | 实际结果 |
|---|---|
| reset 查表、朝向 | 4 项和 12 项通过，含 CPU/CUDA |
| 轨迹分组 | CPU/CUDA 各四组 PASS；七种轨迹状态／输出／随机数一致 |
| 局部 reset、Critic | 100 步真实物理及 Critic 专项完成 |
| PPO smoke | 64 环境、3 次更新；loss／权重有限，Actor 参数变化，checkpoint 重载输出一致 |
| 2048 环境 ordinary | 均值 119.24→107.40 ms；中位数 118.53→118.75 ms，基本不变 |
| 2048 环境 partial reset | 均值 205.62→164.75 ms（-19.9%） |
| 2048 环境 curriculum reset | 均值 340.93→224.87 ms（-34.0%） |
| 独立分组函数 | 2048 环境、42 组，3 次预热＋5 次测量中位数；1／64 个环境变化为 86.891／135.522→1.394／30.942 ms |

早期 256 环境探索计时有波动；最初组合基准／PPO 探针未到达 PPO 完成标记，脚本处理 no_grad 缓冲并显式打印异常后，独立 PPO smoke 才通过。历史失败未作成功证据，详见摘要。旧 [Critic 专项日志](<D:/zerozero_code/fruit-dronefly/logs/validation/critic_ground_truth_2026-09-21.log>) 和 [局部 reset 日志](<D:/zerozero_code/fruit-dronefly/logs/validation/partial_reset_2026-09-21.log>) 保留各自历史范围。

每次新修改重新记录解释器、源码基线／差异、命令、配置、结果和完成标记，不能继承上次 PASS。未来统一 runner／CI 还需传播失败退出码、设置超时并归档日志，目前未实现。

## 如何扩展为测试模块

采用“固定输入 → 调用真实代码 → 检查独立预期”的结构，优先验证状态隔离、几何解析值和输出一致性，不把实现公式复制成唯一依据。新行为和新增边界需要补充相应断言，测试配置简化要写明。

## 验证边界

这是一轮有界 A/B 与局部函数测量，不能换算成长期训练吞吐、收敛或模型性能提升。默认 4096 环境、远程材质、完整课程、其他机型、play／导出和长期训练未验证。CUPTI 初始化警告意味着没有完整 CUDA kernel timeline；CPU 算子跟踪中未出现 nonzero 不等于全链路没有同步。下一物理步对照采用指定容差、命令对照固定未来噪声，不承诺长时物理轨迹逐位一致。
