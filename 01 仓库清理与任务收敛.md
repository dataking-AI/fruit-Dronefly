---
tags:
  - fruit-dronefly
  - 工程审查
类型: 工程改动
工程状态: 已实施
文档审查状态: 已完成本轮静态审查
代码实施状态: 清理与迁移已提交 f7f085d；后续 CUDA 优化另见 P12
运行验证状态: 部分通过（注册与 64 环境 PPO 已跑通；打包 wheel 未验证）
核查日期: 2026-09-22
文档修订: 独立接手版（最小骨架与FlyDrones扩展）
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 仓库清理与任务收敛

[[07 工程上下文与接手约定|工程上下文与接手约定]] · [[README|阅读入口]]

[[00 改动总览|返回总览]] · [[02 策略网络与 PPO 接入|下一模块]]

> [!info] 路径约定
> 下表均相对代码仓库 D:\zerozero_code\fruit-dronefly。表中 P 指**迁移前**的源码根 source/DroneFollow/DroneFollow（已不存在）；迁移后的包根为 source/fruit_dronefly/fruit_dronefly。
>
> **清理与迁移已包含在 f7f085d；早期计划中的 Hummingbird／Iris 删除没有执行，当前两种资产仍保留。**表格保留原计划表述作为对照，实际结果见本页"包名统一决策"与 [[05 实施顺序与验收#顺序与证据]]。

## 保留与修改

| 路径／模块                                                    | 操作                                     | 改后用途及同步影响                                              |
| -------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------ |
| P/tasks/dronefollow                                      | 重命名为 P/tasks/fruit_dronefly            | 保留 v0 的 commands、mdp、terrains、agents；更新导入和 entry point |
| dronefollow_env_cfg_v0.py                                | 重命名为 fruit_dronefly_env_cfg.py         | 拟类名 FruitDroneflyEnvCfg；保持 v0 任务数值与动作语义                |
| tasks/fruit_dronefly/__init__.py（拟）                      | 注册新 ID                                 | 唯一新 ID IsaacLab-FruitDronefly-v0；删除旧 ID 注册             |
| P/common/robot/quadcopter                                | 保留                                     | DRONE_CFG 与实际引用的本地 USD／资产依赖                            |
| P/common/controller                                      | 保留 LeePositionAndYawController、配置及基类依赖 | 不重新调控制器参数                                              |
| P/common/actions/action.py、action_cfg.py                 | 精简                                     | 仅保留首版 LVController＋velocity 执行路径及必要状态、reset            |
| P/common/terrains、common/utils                           | 按调用依赖保留                                | v0 轨迹、地形课程、坐标数学、任务仍调用的显示工具                             |
| scripts/rsl_rl/train.py、play.py、cli_args.py、list_envs.py | 保留并适配                                  | 新任务名、Actor 注册、日志路径和导出命名                                |
| scripts/rsl_rl/plot.py、hud_overlay.py                    | 保留 v0 有用部分并清理旧分支                       | play 当前依赖 HUD；不能只删文件而保留导入                              |
| setup.py、extension.toml、包初始化                             | 更新元数据与导出                               | Python 包统一为 fruit_dronefly；清掉已删模块与无关依赖                           |
| README.md、任务说明、.gitignore（拟新增）                           | 重写／新增                                  | 只说明新任务；忽略缓存、安装产物、日志和训练输出                               |

> [!warning] 删除 PD 分支不等于删除全部 pd 命名字段（**实施已偏离本条，见下**）
> 原方案：速度模式仍通过 cfg.pd_command_name 读取 heading 参考目标，首版保留这个兼容字段，不直接删。

> [!important] 实施偏离记录（2026-09-21）
> **实际做法与上条相反：`pd_command_name` 字段已删除，heading 路径改读 `yaw_command_name`。** 该条 warning 的事实判断是正确的——基线 `action.py:222` 在 `world_frame == "heading"` 分支确实读 `cfg.pd_command_name`；新实现 `action.py:200` 改为 `cfg.yaw_command_name`。
>
> **行为等价性已核实**：两字段在基线中都默认 `"target_pose"`（基线 `action_cfg.py:61`）；v0 环境配置**从未显式设置** `pd_command_name`，只设 `yaw_command_name="target_pose"`（基线 `dronefollow_env_cfg_v0.py:112`），走的是同一默认值；唯一显式设过 `pd_command_name` 的是 v1（基线 `dronefollow_env_cfg_v1.py:123`），而 v1 本轮删除。故对保留任务行为无影响。
>
> 这是"实现偏离已定方案"，按 [[AGENTS]] 规则记录事实与影响，不改写为原设计决策。选择合并字段的理由（减少一个仅历史遗留的别名）未经事前确认，若后续需要 heading 与 yaw 使用不同命令项，需重新引入独立字段。

## 删除清单

| 路径／内容 | 删除理由 | 必须同步处理 |
|---|---|---|
| P/tasks/hover | 首版只保留 v0 派生任务 | 注册、任务自动发现、旧文档与脚本选择 |
| P/tasks/dronefollow_mpc | 不使用 RL 调 MPC 代价权重 | MPC v0/v1 注册和脚本分支 |
| dronefollow_env_cfg_v1.py、PPORunnerCfgV1 | 删除 PD 增益策略 | v1 专属观测、奖励、动作状态及引用 |
| P/common/actions/mpc_action*、mpc_solver* | 首版三维速度直达 Lee | common/actions/__init__.py 导出与求解器专属依赖 |
| P/common/controller/lee_rotor_controller* | 当前使用 collective-thrust Lee 路径 | controller 导出、action RotorController 分支及模型加载函数 |
| P/common/controller/tuning | 不保留历史独立调参程序及图片 | 源文件引用与旧文档 |
| P/common/robot/hummingbird、iris | 历史拟删除项；实际仍保留，不列为已删除 | 当前 robot 导出和 setup.py 仍包含配置／资产；默认任务为 quadcopter，其他机型训练未验收 |
| pretrained | 旧任务 checkpoint 与视频不作为新策略成果 | README、play 的旧模型说明 |
| tasks/hover.md、tasks/dronefollow_mpc.md | 对应任务移除 | 新任务说明替代原 dronefollow.md |
| 所有 __pycache__、*.pyc、复制的 *.egg-info | 生成产物 | .gitignore；不纳入源码提交 |
| 模板 CHANGELOG 等无关文档 | 不属于新任务维护记录 | 扩展元数据和链接 |

公共模块内按“保留任务＋保留脚本”的调用闭包清理；不能仅按名字含 tracking、PD 或 hover 判定是否删除。奖励与地形默认数值保持 v0，清理不夹带任务重设计。

## 注册与安装审查

保留单一环境 ID，通过策略配置区分测试图和 MLP；不新增两套物理环境。拟新实验名 fruit_dronefly_v0_sparse／fruit_dronefly_v0_mlp，去掉 h151；新旧 checkpoint 不混用。

已确定 Python 包名统一为 fruit_dronefly，避免与原 dronefollow 的 DroneFollow 安装混淆。实施验收使用专用环境安装新仓库，并核对 fruit_dronefly.__file__ 指向新代码位置；新入口不导入旧 DroneFollow 或建立兼容 alias。当前源码与安装已改为新名，清理迁移包含在 f7f085d；旧 DroneFollow 安装共存但不由新入口导入。

## 改动记录与验收

- **源码位置**：上表及页末链接。
- **当前行为**（2026-09-21 后）：唯一 `IsaacLab-FruitDronefly-v0`；源码树 `source/fruit_dronefly/fruit_dronefly/`；Hover／MPC／v1／Rotor／tuning／pretrained 已清理；Hummingbird／Iris 仍保留配置、导出和资产，不能作为删除成功项。
- **计划操作**：清理、重命名及脚本收敛已执行；Hummingbird／Iris 的保留与早期计划差异按上表记录。
- **改后行为**：新任务可创建并训练（64 环境 PPO 60 轮，reward -0.53 → 0.55）；回放未验证。
- **关联影响**：包发现、资产打包、任务自动导入、HUD、绘图、日志和安装路径。
- **验收条件**：新任务注册一次 ✅；无悬空导入 ✅；旧 ID 不再由本包注册 ✅；USD 可加载 ✅（环境实跑加载成功）；wheel 打包后 USD 可访问 ⬜ 未验证。
- **审查状态**：工程已实施并提交 f7f085d；运行验证部分通过。

- [x] 确认待删文件路径解析后均位于新代码仓库内。（删除均发生在 `D:\zerozero_code\fruit-dronefly`）
- [x] 核对原 dronefollow 仓库不在任何清理目标中。（`D:\zerozero_code\dronefollow` 工作区干净，未被改动）
- [ ] 确认打包后任务子包和 USD 仍可访问。（editable 安装下可访问；**wheel 构建未执行**，`drone.usd` 是否进入产物无证据）
- [ ] 完成 [[05 实施顺序与验收|环境回归验收]]。（阶段 1／2／4 部分通过，阶段 5 未执行）

后续扩展不要求恢复上述旧任务，见 [[04 后续升级/00 升级依赖与实施边界]]。

## 对外部复用仓库的处理

D:\repository\FlyDrones、CAVEclient、neuprint-python 和 flygym 都是只读参考，均不属于本次清理目标。FlyVis 当前未在该目录发现。

首版不新增 FlyDrones／FlyVis 的运行依赖，不导入 FlyDrones 的 Brain 或 Pilot。后续将图预处理放在单独的可选工具依赖中，训练只读取导出的图资产；不把整个上游仓库复制到源码。元数据保留上游版本、许可证和数据出处。

审查入口：[[06 本地代码库与复用清单]] · [[04 后续升级/06 FlyDrones 连接组数据适配]]。

## 已存在源码入口

以下是核查时实际存在的文件；正文中的“拟新增”文件不在此表冒充现状。

迁移后路径（2026-09-21 核查；旧 `source/DroneFollow/...` 路径已不存在）：

| 源码位置（相对代码仓库） | 依据 |
|---|---|
| [source/fruit_dronefly/setup.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/setup.py>) | 已存在；`name="fruit-dronefly"` + `find_packages()` |
| [source/fruit_dronefly/config/extension.toml](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/config/extension.toml>) | 已存在；已迁移 |
| [source/fruit_dronefly/fruit_dronefly/tasks/__init__.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/__init__.py>) | 已存在 |
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/__init__.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/__init__.py>) | 已存在；唯一 `gym.register`，id 在 `:15` |
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py>) | 已存在；`FruitDroneflyEnvCfg` |
| [source/fruit_dronefly/fruit_dronefly/common/actions/action.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/common/actions/action.py>) | 已存在；P02／P03／P05 修复所在 |
| [source/fruit_dronefly/fruit_dronefly/common/controller/lee_controller_position_and_yaw.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/common/controller/lee_controller_position_and_yaw.py>) | 已存在；`max_fb_acc` 夹紧 |
| [scripts/rsl_rl/list_envs.py](<D:/zerozero_code/fruit-dronefly/scripts/rsl_rl/list_envs.py>) | 已存在；2026-09-21 实跑通过 |

## 包名统一决策

**包名统一已于 2026-09-21 实施完成并实跑验证。** 旧"暂留 DroneFollow"决策只为减少首版全量导入改动；因 env_isaaclab 的 editable 安装指向原 dronefollow、共用包名会让脚本静默导入错误仓库，故改为直接迁移。

| 对象 | 已确定目标 | 实际现状（2026-09-21） |
|---|---|---|
| 仓库名／pip distribution | fruit-dronefly | ✅ `setup.py: name="fruit-dronefly"`；已安装 0.1.0 |
| Python import 包 | fruit_dronefly | ✅ `find_spec` origin 指向新源码树 |
| 扩展源码目录 | source/fruit_dronefly/fruit_dronefly | ✅ 已迁移；旧路径不存在 |
| 任务子目录 | tasks/fruit_dronefly | ✅ 已迁移；配置类 `FruitDroneflyEnvCfg` |
| Gym ID | IsaacLab-FruitDronefly-v0 | ✅ `list_envs.py` 实跑仅此一个 |
| 旧包隔离 | 新入口不导入旧包、不建 alias | ✅ 0 处残留引用；但旧 `DroneFollow` 0.1.0 仍安装于环境中（指向旧仓库），需注意不要在同一脚本混用 |

难度为中小规模重构：主要是受控目录移动和符号引用同步，不重写物理、观测、奖励、控制器或 PPO。旧任务、MPC、tuning 等本来要删除，可先收敛再迁移，减少无用替换。当前无须兼容旧 checkpoint，迁移窗口较合适。

拟操作顺序：

1. 记录现存和未跟踪源码基线，按既定清单完成收敛，不改原 dronefollow 仓库。
2. 移动外层扩展目录与内层 Python 包，更新保留代码中的 `from/import DroneFollow`、train/play/list_envs/plot 入口；任务注册使用 `__name__` 的部分随包名变化，但仍核对字符串配置入口。
3. 将 setup.py 的 distribution 设为 fruit-dronefly，使用 find_packages 或等价包发现纳入子包；extension.toml 的 python.module 改为 fruit_dronefly，同步标题、描述和项目来源，避免继续冒充 IsaacLab 模板。
4. 明确打包 USD 等资源；四旋翼当前使用相对 `__file__` 定位，迁移后仍应验证 editable 与构建 wheel 的资源可访问性。不能只替换 packages=["DroneFollow"] 为一个新顶层名字就认为子包和资产已打包。
5. 在专用环境安装新发行包，核对 fruit_dronefly 的解析位置。原 DroneFollow 可继续供旧项目使用；新脚本不再导入它，不增加兼容 alias，避免意外注册旧任务。
6. 同步策略模型 class_name、配置路径、日志命名、文档拟议路径和安装命令；实际源码引用在迁移完成后再更新为真实新路径。

验收（2026-09-21 逐项结果）：新包解析指向 fruit-dronefly ✅；仅由新包注册新 ID ✅；保留的有效代码无旧包导入 ✅；train/play 同配置 ✅（`clip_actions` 两端同读 `agent_cfg`，均为 `null`）；环境与 PPO 回归 ✅（64 环境、60 迭代）；资产和任务子包**打包**可用 ⬜（editable 可用，wheel 未构建）；保存重载与导出回归 ⬜（`model_0.pt` 已写出，未做重载与 JIT／ONNX 导出）。

- **源码位置**：setup.py、extension.toml、包初始化、保留模块导入与 scripts/rsl_rl 入口。
- **当前行为**：两包已有独立 import 身份（`fruit_dronefly` / `DroneFollow`），各指向自己的仓库。
- **计划操作**：按已确定的统一方案实施目录、导入、元数据与配置路径迁移。**已执行**。
- **改后行为**：同名 editable 冲突已消除；新脚本不再可能静默导入旧仓库。
- **关联影响**：任务发现、类路径、资源打包、checkpoint 配置、文档路径。
- **验收条件**：命名、注册与运行回归 ✅；打包（wheel）与导出回归 ⬜。
- **审查状态**：**已实施并部分运行验证（2026-09-21）**；清理已提交 f7f085d；wheel 打包与导出未验证。

2026-09-22 回填：本轮 CUDA 优化另有 3 次 PPO smoke 与 checkpoint 重载一致性证据，见 [[05 实施顺序与验收#CUDA 采样优化专项（P12）]]；上方首次迁移验收保留其当时的执行范围，wheel、play／导出仍未验证。
