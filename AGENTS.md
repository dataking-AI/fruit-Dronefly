# FruitDronefly 文档接手规则

本目录是工程审查知识库；实施源码默认位于 `D:\zerozero_code\fruit-dronefly`。无需历史对话即可按以下顺序接手：

1. 读取 [工程上下文与接手约定](07%20工程上下文与接手约定.md)。
2. 读取 [改动总览](00%20改动总览.md) 和 [本地代码库与复用清单](06%20本地代码库与复用清单.md)。
3. 根据当前任务读取具体模块，再读 [实施顺序与验收](05%20实施顺序与验收.md)。完整导航见 [README](README.md)。

- 先复核源码、版本和当前状态，再执行当前用户请求；阅读文档本身不等于要求实施全部阶段。
- 区分已存在源码、拟新增模块、首版范围和后续升级，不把未执行的验收写成通过。
- 发生文档与源码差异时，记录事实与影响；不能把实现偏差自动改写成设计决策。
- 本库集中维护方案，源码 README/AGENTS 只维护入口。实施后同步对应条目的状态、接口和证据。
- 外部参考库默认只读；路径缺失或版本不符时先报告事实，不自行以另一版本替代。既有用户授权和当前任务范围优先。
- Windows 中文文档写入必须遵循 [[07 工程上下文与接手约定#注意事项：Windows 中文文档写入]]：使用 UTF-8 临时脚本、写前核对全部锚点、写后回读验证；避免中文经默认命令管道转码。
- 保留 `.obsidian` 配置。当前首版边界及版本核查事项以工程上下文页为准。

规则是否自动载入取决于 agent 工具；[README](README.md) 提供同一阅读入口。
