---
tags:
  - fruit-dronefly
  - 工程审查
类型: 工程改动
工程状态: 首版清理与 CUDA 采样优化已实施，图 Actor 未开始
文档审查状态: 已完成本轮静态审查
代码实施状态: 清理、迁移与局部 reset 已提交 f7f085d；CUDA 优化和新增测试未提交
运行验证状态: 专项通过（局部 reset、CUDA 优化与 PPO smoke；play／导出／4096 规模未验证）
核查日期: 2026-09-22
文档修订: 独立接手版（最小骨架与FlyDrones扩展）
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 改动总览

[[07 工程上下文与接手约定|工程上下文与接手约定]] · [[README|阅读入口]]

> [!info] 审查对象
> 代码位于 D:\zerozero_code\fruit-dronefly；本知识库位于 D:\personal_code\fruit-dronefly。源码核查日期：2026-09-21。
>
> **仓库清理、包名迁移、局部 reset 修复和 CUDA 采样路径优化已实施并完成专项验证；图 Actor 及后续升级仍未实施。** 文档已落库仍不代表代码已实现——请按下表逐项状态判断，证据见 [[07 工程上下文与接手约定#2026-09-21 实施与运行证据]]。


## 接手入口与方案依据

[[07 工程上下文与接手约定]] 独立记录研究目标、已定决策、接口摘要和待核查项。本页及各模块共同构成首版实施基线，无需获取历史对话中的 plan。仓库索引负责统一维护默认路径、来源和版本。

## 本次改动边界

| 项目     | 已确定方案                                                       | 实施状态  |
| ------ | ----------------------------------------------------------- | ----- |
| 仓库     | 从复制的 DroneFollow 收敛为最小任务；Python 包名统一为 fruit_dronefly | **已实施并提交 f7f085d**（本轮 CUDA 优化与新增测试未提交）   |
| 环境     | 唯一新任务 IsaacLab-FruitDronefly-v0，继承原 v0 低维跟踪环境               | **已实施并实跑**（64 环境、约 38 s 仿真）   |
| Actor  | 极小、无记忆、分层稀疏测试图；固定拓扑、训练存在边权                                  | 未实施（当前为 MLPModel）   |
| Critic | 独立 MLP；沿用 PPO                                               | **已实施**（`MLPModel`＋`PPO`＋`OnPolicyRunner`）   |
| 输出     | 三维速度；原 heading 目标连线系；传统 Lee 控制器                             | **已实施**（`tanh` 映射、`world_frame="heading"`、Lee 集体推力）   |
| 后续     | FlyVis、相机、嗅觉、真实连接组、循环状态、部署                                  | 后续未实施 |

本次首版不安装 FlyVis，不加入相机或气味场，不实现循环策略，不添加脉冲编码。测试图只验证工程接口，不能作为果蝇连接结构的研究证据。

## 首版实施基线 与 FlyDrones 扩展的对应关系

首版实施基线 是本库的首版实施基线。本次扩展只改变后续真实连接组的数据复用路线，不把首版升级为脉冲网络或全脑仿真。

| 原始模块    | 首版保持                                           | 结合 FlyDrones 的后续改动                      |
| ------- | ---------------------------------------------- | --------------------------------------- |
| 清理和任务结构 | 唯一新 ID；包名统一为 fruit_dronefly；保留 v0 物理、Lee 与三维速度 | 不接管 FlyDrones runtime，不恢复旧任务            |
| 连接组策略接口 | 约几十节点的固定测试图；连续值、无记忆、可训练边权                      | 复用 FlyDrones 数据层提取真实图，转换为项目侧可训练参数       |
| PPO 集成  | 自定义 Actor、独立 MLP Critic、普通 rollout             | 真实图有循环时单独扩展状态和序列训练                      |
| 视觉      | 首版低维代理观测；FlyVis 不作为运行依赖                        | FlyVis 负责视觉计算；FlyDrones 分组配置作为感觉／运动节点候选 |
| 动作与控制   | 三维高层速度、原 heading、传统控制器                         | 新读出由 PPO 学习，不沿用固定 cruise／escape 规则      |
| 验证      | 静态、100 步环境、梯度、PPO、保存回放和 MLP 对照                 | 另加数据方向、节点身份、子图可达性、视觉衔接验证                |

### 数据准备与运行链路分开

~~~mermaid
flowchart TD
    T["首版：极小测试图"] --> P["项目侧 PyTorch Actor＋PPO"]
    D["后续：MaleCNS 原始数据"] --> F["复用 FlyDrones 加载／分组／子图"]
    F --> A["离线图资产：节点 ID、边、groups、roles、版本"]
    A --> P
    I["后续：Isaac Lab 相机"] --> V["可选 FlyVis 视觉模型"]
    V --> X["显式空间／类型／节点映射"]
    X --> P
    O["后续：气体测量"] --> X
    P --> R["可训练三维运动读出"]
    R --> L["原 DroneAction＋Lee 执行链"]
~~~

离线图资产不携带 FlyDrones 的 LIF 仿真器。FlyVis 节点与 MaleCNS 节点没有天然相同的 ID，映射须显式审查；图有循环时 Actor 内部也会变化，不能承诺只是替换文件。尽量保持环境侧三维动作与训练入口，输入维度／状态变化需新配置和 checkpoint。

## 新增审查入口

- [[04 后续升级/06 FlyDrones 连接组数据适配|FlyDrones 数据适配]]：具体复用函数、离线流程、导出资产及验证。
- [[04 后续升级/07 感觉运动节点映射与可训练读出|感觉与运动映射]]：复用哪些神经元分组，如何衔接 FlyVis、嗅觉与三维动作。
- [[06 本地代码库与复用清单|本地源码与版本清单]]：D:\repository 中已核查资源、源码位置、用途及不复用部分。

## 当前架构与改后架构

清理、包名迁移、Critic 真值调整与局部 reset 修复已包含在提交 f7f085d；清理前基线为 b87f6a2。当前仅注册 IsaacLab-FruitDronefly-v0，本轮 CUDA 优化和新增测试尚未提交。

~~~mermaid
flowchart LR
    C["历史：复制仓库五个任务 ID"] --> R["已完成：唯一 FruitDronefly-v0"]
    R --> O["保留：低维观测 B×19"]
    O --> P["新增：测试图 Actor"]
    P --> A["保留：三维 raw action"]
    A --> V["tanh 与每轴速度缩放"]
    V --> H["heading → world；自动偏航"]
    H --> L["保留：Lee 控制器"]
    L --> E["Isaac Lab 无人机环境"]
    E --> O
    E -. "批量经验与奖励" .-> T["PPO 与独立 Critic"]
    T -. "更新可训练参数" .-> P
~~~

图中清理已完成，测试图 Actor 仍是计划；实线为运行数据流，虚线为训练反馈。PPO 不通过物理仿真或 Lee 控制器求梯度。

## 审查入口

| 模块 | 审查重点 |
|---|---|
| [[07 工程上下文与接手约定]] | 目标、决策、接手规则与待核查项 |
| [[06 本地代码库与复用清单]] | 默认仓库、官方入口、版本与用途 |
| [[01 仓库清理与任务收敛]] | 删除清单、依赖、注册和命名 |
| [[02 策略网络与 PPO 接入]] | 测试图、训练接口、保存与回放 |
| [[03 观测动作与控制接口/00 接口改动总览|观测动作与控制接口]] | 观测、动作、坐标、控制四个二级页面 |
| [[04 后续升级/00 升级依赖与实施边界|后续升级]] | 每个未来功能接入位置、前置条件与边界 |
| [[05 实施顺序与验收]] | 实施顺序、检查项与证据记录 |
| [[工程改动地图.canvas|工程改动地图]] | 模块数据流与升级依赖 |

## 本批审查记录

- **源码位置**：本页末尾入口及各模块的源码表。
- **当前行为**：单任务 MLP 策略，可训练（reward 由负转正）。
- **计划操作**：清理任务 ✅；局部 reset 与采样热路径 ✅；增加可训练稀疏测试图 Actor ⬜；保留底层执行链 ✅。
- **改后行为**：最小环境已可用 MLP 策略运行；测试图路径待接入。
- **关联影响**：导入、注册、脚本过滤、实验目录、导出名、局部 reset 和采样性能测试均已记录（本轮同步入口、总览、验收、接手、仓库索引、测试和相关接口页）。
- **验收条件**：见 [[05 实施顺序与验收]]；局部 reset、采样路径和 PPO smoke 有专项证据，图 Actor、play／导出和默认规模仍未验收。
- **审查状态**：清理阶段已实施并部分验证；图 Actor 阶段未实施。

- [x] 删除范围与保留范围一致。（宽域扫描 0 残留；保留项均在实跑中被加载）
- [x] 首版与后续升级边界明确。（图 Actor／FlyVis／真实连接组仍标注未实施）
- [x] 没有把已知目标跟踪误称为未知源自主搜索。（当前任务仍为已知目标跟踪，未改写表述）

## 已存在源码入口

以下是核查时实际存在的文件；正文中的“拟新增”文件不在此表冒充现状。

| 源码位置（相对代码仓库） | 依据 |
|---|---|
| [README.md](<D:/zerozero_code/fruit-dronefly/README.md>) | 已存在；2026-09-21 已重写 |
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py>) | 已迁移；`FruitDroneflyEnvCfg`，2026-09-21 实跑加载 |
| [source/fruit_dronefly/fruit_dronefly/common/actions/action.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/common/actions/action.py>) | P02／P03／P05 修复所在 |

## 环境兼容性复核

原 v0 的 100 步、2 次 PPO、保存重载及 JIT／ONNX 导出探针见 [[08 运行环境兼容性与版本方案]]。

**2026-09-21 更新**：新任务已实施并实跑——`IsaacLab-FruitDronefly-v0` 注册唯一、64 环境 PPO 60 轮、reward -0.53 → 0.55。包名迁移已完成（`fruit-dronefly` 0.1.0，`find_spec` 指向新树）。RSL-RL 5.0.1 的独立 actor/critic 模型配置已按实际使用：`MLPModel` × 2 ＋ `GaussianDistribution` ＋ `PPO` ＋ `OnPolicyRunner`。**图 Actor 仍未实施**；新任务的 play／导出、默认规模和长期训练仍未验证，详见 [[05 实施顺序与验收#顺序与证据]]。

## 2026-09-22 CUDA 采样优化实施与证据

本轮完成三条采样热路径优化：reset 轨迹起点和方向改为地形侧 CUDA 查找表；trajectory.py 的 yaw 改为固定形状 torch.where；commands.py 按受影响环境增量刷新轨迹组并复用未变化组状态。

2048 环境有界 A/B（10 次预热、30 次测量、计时边界显式同步 CUDA）：

| 场景 | 修改前均值 | 修改后均值 | 均值变化 |
|---|---:|---:|---:|
| ordinary | 119.24 ms | 107.40 ms | -9.9% |
| partial reset | 205.62 ms | 164.75 ms | -19.9% |
| curriculum reset | 340.93 ms | 224.87 ms | -34.0% |

独立轨迹分组测量：1 个环境变化 86.891 → 1.394 ms；64 个环境变化 135.522 → 30.942 ms。CPU/CUDA 正确性、真实局部 reset 100 步、Critic 观测和 3 次 PPO smoke 均通过，摘要见 [CUDA 优化验证摘要](<D:/zerozero_code/fruit-dronefly/logs/validation/cuda_optimization_20260921/summary.md>)。

这是单次有界 A/B 和局部函数测量，不能推出长期训练吞吐、收敛、默认 4096 环境、远程材质或完整 CUDA kernel timeline；普通场景中位数基本不变。

源码位置：[terrain_importer.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/common/terrains/terrain_importer.py>)、[events.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/events.py>)、[trajectory.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/commands/trajectory.py>)、[commands.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/commands/commands.py>)。测试与复现方式见 [[09 自动化测试与回归检查]]。
