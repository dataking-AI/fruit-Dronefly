---
tags:
  - fruit-dronefly
  - 工程审查
类型: 工程改动
工程状态: 未实施
文档审查状态: 已完成本轮静态审查
代码实施状态: 未实施
运行验证状态: 未执行
核查日期: 2026-09-20
文档修订: 独立接手版（最小骨架与FlyDrones扩展）
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 仓库清理与任务收敛

[[07 工程上下文与接手约定|工程上下文与接手约定]] · [[README|阅读入口]]

[[00 改动总览|返回总览]] · [[02 策略网络与 PPO 接入|下一模块]]

> [!info] 路径约定
> 下表均相对代码仓库 D:\zerozero_code\fruit-dronefly。P 表示当前源码 source/DroneFollow/DroneFollow；保留项迁移后的目标包根为 source/fruit_dronefly/fruit_dronefly。所有删除、移动和新增均未执行。

## 保留与修改

| 路径／模块                                                    | 操作                                     | 改后用途及同步影响                                              |
| -------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------ |
| P/tasks/dronefollow                                      | 重命名为 P/tasks/fruit_dronefly            | 保留 v0 的 commands、mdp、terrains、agents；更新导入和 entry point |
| dronefollow_env_cfg_v0.py                                | 重命名为 fruit_dronefly_env_cfg.py         | 拟类名 FruitDroneflyEnvCfg；保持 v0 任务数值与动作语义                |
| tasks/fruit_dronefly/__init__.py（拟）                      | 注册新 ID                                 | 唯一新 ID IsaacLab-FruitDronefly-v0；删除旧 ID 注册             |
| P/common/robot/quadcopter                                | 保留                                     | DRONE_CFG 与实际引用的本地 USD／资产依赖                            |
| P/common/controller                                      | 保留 LeePositionAndYawController、配置及基类依赖 | 不重新调控制器参数                                              |
| P/common/actions/action.py、action_cfg.py                 | 精简                                     | 仅保留首版 LVController＋velocity 执行路径及必要状态、reset            |
| P/common/terrains、common/utils                           | 按调用依赖保留                                | v0 轨迹、地形课程、坐标数学、任务仍调用的显示工具                             |
| scripts/rsl_rl/train.py、play.py、cli_args.py、list_envs.py | 保留并适配                                  | 新任务名、Actor 注册、日志路径和导出命名                                |
| scripts/rsl_rl/plot.py、hud_overlay.py                    | 保留 v0 有用部分并清理旧分支                       | play 当前依赖 HUD；不能只删文件而保留导入                              |
| setup.py、extension.toml、包初始化                             | 更新元数据与导出                               | Python 包统一为 fruit_dronefly；清掉已删模块与无关依赖                           |
| README.md、任务说明、.gitignore（拟新增）                           | 重写／新增                                  | 只说明新任务；忽略缓存、安装产物、日志和训练输出                               |

> [!warning] 删除 PD 分支不等于删除全部 pd 命名字段
> 速度模式仍通过 cfg.pd_command_name 读取 heading 参考目标。首版保留这个兼容字段并说明用途，或在同一次改动中完成全引用迁移；不能直接删字段。当前方案采用保留字段，不另做接口重命名。

## 删除清单

| 路径／内容 | 删除理由 | 必须同步处理 |
|---|---|---|
| P/tasks/hover | 首版只保留 v0 派生任务 | 注册、任务自动发现、旧文档与脚本选择 |
| P/tasks/dronefollow_mpc | 不使用 RL 调 MPC 代价权重 | MPC v0/v1 注册和脚本分支 |
| dronefollow_env_cfg_v1.py、PPORunnerCfgV1 | 删除 PD 增益策略 | v1 专属观测、奖励、动作状态及引用 |
| P/common/actions/mpc_action*、mpc_solver* | 首版三维速度直达 Lee | common/actions/__init__.py 导出与求解器专属依赖 |
| P/common/controller/lee_rotor_controller* | 当前使用 collective-thrust Lee 路径 | controller 导出、action RotorController 分支及模型加载函数 |
| P/common/controller/tuning | 不保留历史独立调参程序及图片 | 源文件引用与旧文档 |
| P/common/robot/hummingbird、iris | 首版只用 quadcopter | robot 导出、控制器配置及所有资产引用 |
| pretrained | 旧任务 checkpoint 与视频不作为新策略成果 | README、play 的旧模型说明 |
| tasks/hover.md、tasks/dronefollow_mpc.md | 对应任务移除 | 新任务说明替代原 dronefollow.md |
| 所有 __pycache__、*.pyc、复制的 *.egg-info | 生成产物 | .gitignore；不纳入源码提交 |
| 模板 CHANGELOG 等无关文档 | 不属于新任务维护记录 | 扩展元数据和链接 |

公共模块内按“保留任务＋保留脚本”的调用闭包清理；不能仅按名字含 tracking、PD 或 hover 判定是否删除。奖励与地形默认数值保持 v0，清理不夹带任务重设计。

## 注册与安装审查

保留单一环境 ID，通过策略配置区分测试图和 MLP；不新增两套物理环境。拟新实验名 fruit_dronefly_v0_sparse／fruit_dronefly_v0_mlp，去掉 h151；新旧 checkpoint 不混用。

已确定 Python 包名统一为 fruit_dronefly，避免与原 dronefollow 的 DroneFollow 安装混淆。实施验收使用专用环境安装新仓库，并核对 fruit_dronefly.__file__ 指向新代码位置；新入口不导入旧 DroneFollow 或建立兼容 alias。当前源码与安装仍为旧名，本次只更新文档决策。

## 改动记录与验收

- **源码位置**：上表及页末链接。
- **当前行为**：五个环境注册、公共导出含 MPC／其他机器人，复制内容未跟踪。
- **计划操作**：按表删除、重命名、裁剪导出和脚本分支。
- **改后行为**：唯一新任务可创建、训练、回放；保留 v0 的物理与控制行为。
- **关联影响**：包发现、资产打包、任务自动导入、HUD、绘图、日志和安装路径。
- **验收条件**：新任务注册一次；无悬空导入；USD 可加载；旧 ID 不再由本包注册。
- **审查状态**：工程未实施。

- [ ] 确认待删文件路径解析后均位于新代码仓库内。
- [ ] 核对原 dronefollow 仓库不在任何清理目标中。
- [ ] 确认打包后任务子包和 USD 仍可访问。
- [ ] 完成 [[05 实施顺序与验收|环境回归验收]]。

后续扩展不要求恢复上述旧任务，见 [[04 后续升级/00 升级依赖与实施边界]]。

## 对外部复用仓库的处理

D:\repository\FlyDrones、CAVEclient、neuprint-python 和 flygym 都是只读参考，均不属于本次清理目标。FlyVis 当前未在该目录发现。

首版不新增 FlyDrones／FlyVis 的运行依赖，不导入 FlyDrones 的 Brain 或 Pilot。后续将图预处理放在单独的可选工具依赖中，训练只读取导出的图资产；不把整个上游仓库复制到源码。元数据保留上游版本、许可证和数据出处。

审查入口：[[06 本地代码库与复用清单]] · [[04 后续升级/06 FlyDrones 连接组数据适配]]。

## 已存在源码入口

以下是核查时实际存在的文件；正文中的“拟新增”文件不在此表冒充现状。

| 源码位置（相对代码仓库） | 依据 |
|---|---|
| [source/DroneFollow/setup.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/setup.py>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/config/extension.toml](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/config/extension.toml>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/DroneFollow/tasks/__init__.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/DroneFollow/tasks/__init__.py>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/DroneFollow/tasks/dronefollow/__init__.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/DroneFollow/tasks/dronefollow/__init__.py>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/DroneFollow/common/actions/__init__.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/DroneFollow/common/actions/__init__.py>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/DroneFollow/common/controller/__init__.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/DroneFollow/common/controller/__init__.py>) | 已存在；2026-09-20 静态核查 |
| [source/DroneFollow/DroneFollow/common/robot/__init__.py](<D:/zerozero_code/fruit-dronefly/source/DroneFollow/DroneFollow/common/robot/__init__.py>) | 已存在；2026-09-20 静态核查 |
| [scripts/rsl_rl/list_envs.py](<D:/zerozero_code/fruit-dronefly/scripts/rsl_rl/list_envs.py>) | 已存在；2026-09-20 静态核查 |

## 包名统一决策

**用户已确定在清理阶段统一 Python 包名为 fruit_dronefly；文档已同步，源码及安装迁移尚未执行。** 旧“暂留 DroneFollow”决策只为减少首版全量导入改动。现已发现 env_isaaclab 的 editable 安装指向原 dronefollow，新旧项目共用包名会让脚本静默导入错误仓库；没有性能或算法上的理由保留旧名。

| 对象 | 已确定目标 | 当前现状 |
|---|---|---|
| 仓库名／pip distribution | fruit-dronefly | 仓库已有新名，发行包仍 DroneFollow |
| Python import 包 | fruit_dronefly | 当前 DroneFollow；连字符不能出现在 import 标识符中 |
| 扩展源码目录 | source/fruit_dronefly/fruit_dronefly | 当前 source/DroneFollow/DroneFollow |
| 任务子目录 | tasks/fruit_dronefly | 当前 tasks/dronefollow；原清理计划已包含此迁移 |
| Gym ID | IsaacLab-FruitDronefly-v0 | 保持既定新 ID，迁移尚未执行 |

难度为中小规模重构：主要是受控目录移动和符号引用同步，不重写物理、观测、奖励、控制器或 PPO。旧任务、MPC、tuning 等本来要删除，可先收敛再迁移，减少无用替换。当前无须兼容旧 checkpoint，迁移窗口较合适。

拟操作顺序：

1. 记录现存和未跟踪源码基线，按既定清单完成收敛，不改原 dronefollow 仓库。
2. 移动外层扩展目录与内层 Python 包，更新保留代码中的 `from/import DroneFollow`、train/play/list_envs/plot 入口；任务注册使用 `__name__` 的部分随包名变化，但仍核对字符串配置入口。
3. 将 setup.py 的 distribution 设为 fruit-dronefly，使用 find_packages 或等价包发现纳入子包；extension.toml 的 python.module 改为 fruit_dronefly，同步标题、描述和项目来源，避免继续冒充 IsaacLab 模板。
4. 明确打包 USD 等资源；四旋翼当前使用相对 `__file__` 定位，迁移后仍应验证 editable 与构建 wheel 的资源可访问性。不能只替换 packages=["DroneFollow"] 为一个新顶层名字就认为子包和资产已打包。
5. 在专用环境安装新发行包，核对 fruit_dronefly 的解析位置。原 DroneFollow 可继续供旧项目使用；新脚本不再导入它，不增加兼容 alias，避免意外注册旧任务。
6. 同步策略模型 class_name、配置路径、日志命名、文档拟议路径和安装命令；实际源码引用在迁移完成后再更新为真实新路径。

验收：新包解析指向 fruit-dronefly；仅由新包注册新 ID；保留的有效代码无旧包导入；资产和任务子包打包可用；train/play 同配置；环境、PPO、保存重载和导出回归通过。区别旧仓库名的历史引用与残留有效导入，不做无差别全文替换。

- **源码位置**：setup.py、extension.toml、包初始化、保留模块导入与 scripts/rsl_rl 入口。
- **当前行为**：两个仓库同名包；现有 editable 指原工程。
- **计划操作**：按已确定的统一方案实施目录、导入、元数据与配置路径迁移；本轮仅同步文档。
- **改后行为**：新旧项目有独立 import 身份，可避免同名 editable 冲突。
- **关联影响**：任务发现、类路径、资源打包、checkpoint 配置、文档路径。
- **验收条件**：上述命名、打包、注册与运行回归通过。
- **审查状态**：统一命名决策已确认，总览与上下文已同步；原暂留旧名方案仅为历史记录，源码迁移与运行验收未实施。
