---
tags:
  - fruit-dronefly
  - 工程接手
类型: 工程接手
工程状态: 首版清理与 CUDA 采样优化已实施
文档审查状态: 已完成本轮静态审查
代码实施状态: 清理、迁移与局部 reset 已提交 f7f085d；CUDA 优化和新增测试未提交
运行验证状态: 专项通过（局部 reset、CUDA 优化与 PPO smoke；play／导出／4096 规模未验证）
核查日期: 2026-09-22
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 工程上下文与接手约定

[[README|返回阅读入口]] · [[00 改动总览|实施基线]] · [[06 本地代码库与复用清单|仓库与依赖]] · [[05 实施顺序与验收|验收]]

## 工程目标与成功标准

FruitDronefly 研究能否将果蝇连接组的拓扑作为强化学习策略的结构约束：保留指定连接，使用反向传播训练有效边权，通过视觉、气味等输入产生无人机高层三维速度指令，由传统 Lee 控制器完成底层控制。研究重点是连接拓扑及感觉—运动映射，不要求采用脉冲表达。

首版只交付可运行的工程基座：清理复制的 DroneFollow，保留低维目标跟踪任务，用极小连续值无记忆测试图替换 Actor，验证 PPO、保存、回放、导出和三维速度接口。通过标准见验收页的静态检查、100 步环境检查及 PPO 短运行。测试图不是真实果蝇连接组，低维目标观测也不是真实视觉／气味输入；首版不支持连接组性能优越的结论。

长期视觉／气味自主导航的具体任务、奖励、传感器和气体场仍须后续定义。当前有目标信息的跟踪任务不能直接称为未知气源搜索。这里描述研究方向，不额外承诺已设计或已实现的任务。

## 默认工作目录与当前事实

| 对象 | 默认位置与职责 |
|---|---|
| 方案文档 | D:\personal_code\fruit-dronefly；集中维护设计、审查和验收 |
| 实施源码 | D:\zerozero_code\fruit-dronefly；唯一默认功能改动对象 |
| 迁移参考 | D:\zerozero_code\dronefollow；查原有行为，默认不修改 |
| 仿真框架参考 | D:\zerozero_code\IsaacLab；已确认 env_isaaclab 从此加载；固定 commit 见环境兼容性页 |
| 外部研究代码 | D:\repository；FlyDrones、CAVEclient、neuprint-python、flygym、OmniDrones（控制与 reset 参考） |
| 文档样式参考 | D:\personal_code\MPC；编号、双向链接、Mermaid、Canvas 的参考，不是本工程运行依赖 |

截至 2026-09-21，阶段 1 清理与包名迁移已在源码中实施：源码树为 `source/fruit_dronefly/fruit_dronefly/`，仅注册 `IsaacLab-FruitDronefly-v0`，旧五个 ID 与 Hover／MPC／v1／Rotor／tuning 分支已删除。与早期删除方案不同，当前源码仍保留 Hummingbird／Iris 配置、导出和资产打包声明；默认任务使用 quadcopter，其他机型训练适配未验证。`fruit_dronefly` 的 import 解析已指向新源码树，发行包 `fruit-dronefly` 0.1.0 已安装。原 `DroneFollow` 0.1.0 仍安装并指向 `D:\zerozero_code\dronefollow`，新入口不导入它，属可接受共存。

2026-09-22 核对：main 与本地 origin/main 引用均指向 f7f085d22919fab755e2f2f9813d23ecbc735c69；清理、迁移、Critic 和局部 reset 修复已提交。本轮 CUDA 优化及新增测试仍在工作树，验证摘要的 11 个文件 SHA-256 与当前文件一致；f7f085d 是优化前基线，不能单独表示受测的完整工作树。

## 已确定的实施决策

| 决策   | 首版约束与理由                                                            | 详细位置                             |
| ---- | ------------------------------------------------------------------ | -------------------------------- |
| 任务身份 | 唯一 IsaacLab-FruitDronefly-v0；Python 包名统一为 fruit_dronefly（源码及安装已迁移） | [[01 仓库清理与任务收敛]]                 |
| 清理范围 | 保留无人机、地形、轨迹命令、奖励、终止、事件和训练入口；清理 Hover、PD、MPC、无用资产与旧 checkpoint      | [[01 仓库清理与任务收敛]]                 |
| 网络   | 约几十节点的固定分层有向无环测试图，连续激活，可训练有效边权，无跨环境步状态                             | [[02 策略网络与 PPO 接入]]              |
| 学习   | RSL-RL PPO；图 Actor、独立 MLP Critic；主对照仅 PPO＋MLP                      | [[02 策略网络与 PPO 接入]]              |
| 输入   | 当前 19 维低维代理观测；首版不接相机、气体场或 FlyVis                                   | [[03 观测动作与控制接口/01 观测组成与输入映射]]    |
| 动作   | 高层三维速度，沿用目标连线 heading、自动偏航和 Lee                                    | [[03 观测动作与控制接口/00 接口改动总览]]       |
| 后续复用 | FlyDrones 提供离线图数据工具与候选分组；FlyVis 提供后续视觉计算                           | [[04 后续升级/00 升级依赖与实施边界]]         |
| 模型边界 | 不接入 FlyDrones LIF／Pilot／固定 cruise/escape；不添加 SNN 编解码器              | [[04 后续升级/06 FlyDrones 连接组数据适配]] |

具体测试图连边表、图模型的接口实现仍是实施前核定项；RSL-RL 5.0.1 的原生模块路径解析已确认；不得把未选定的细节当作既定事实。未来真实循环图可能需要状态和序列训练，不能承诺只替换数据文件就兼容旧 checkpoint。

## 数据流与接口摘要

B 表示并行环境数。此表用于定位；字段定义、单位和 reset 细节以对应接口页为准。接口发生变化时须同步本表和验收页。

| 边界               | 当前 v0／首版计划                                                         | 详细定义                             |
| ---------------- | ------------------------------------------------------------------ | -------------------------------- |
| Actor 观测         | B×19：速度 3＋四元数 4＋跟踪量 9＋前次动作 3                                       | [[03 观测动作与控制接口/01 观测组成与输入映射]]    |
| Critic 观测与 value | 独立 B×19 观测组，value 对外语义 B×1；当前无噪声、无延迟目标真值（2026-09-21 用户授权调整；专项及 3 次 PPO smoke 通过）                           | [[02 策略网络与 PPO 接入]]              |
| Actor 原始动作       | 三维高斯，训练采样／确定性回放用均值；Actor 不额外 tanh                                  | [[03 观测动作与控制接口/02 策略动作与速度限幅]]    |
| 速度映射             | action 层每轴 5×tanh(a) m/s；原 v0 实测 wrapper clip_actions=None，新配置已核对 train/play 同读 clip_actions=null | [[03 观测动作与控制接口/02 策略动作与速度限幅]]    |
| 坐标与偏航            | heading 为水平目标连线系，+Y 左、+Z 上；非自身 yaw 系；face_target 仍依赖目标真值           | [[03 观测动作与控制接口/03 坐标转换与偏航来源]]    |
| Lee 输入           | 世界系三维速度＋期望 yaw 角，非 yaw rate；输出推力与力矩                                | [[03 观测动作与控制接口/04 Lee 控制器与执行时序]] |
| 时序               | 物理 dt=0.01 s，decimation=4；策略／Lee 25 Hz，物理重复施力 100 Hz；局部 reset 专项已核对每步调用次数   | [[03 观测动作与控制接口/04 Lee 控制器与执行时序]] |
| 梯度               | PPO 更新 Actor／Critic；不通过 Isaac Lab 物理或 Lee 控制器求导                    | [[02 策略网络与 PPO 接入]]              |

## 下一步：基线与依赖核查

接手先复核当前 HEAD、未提交差异与证据。清理和 MLP 环境专项已经完成，下一步按用户范围处理 P07/P08/P09 或图 Actor；阶段 0 记录方法继续沿用 [[05 实施顺序与验收#阶段 0：基线与依赖核查|阶段 0]]。

以下事项是集中索引，各模块仍负责其具体审查要求。这里的“阻塞”指进入对应阶段前须解决，不表示文档工作被阻塞。

| ID | 待核查项与入口 | 影响阶段 | 完成证据 |
|---|---|---|---|
| B01 | ~~实施仓库未跟踪文件、任务注册、版本与导入路径~~ **已闭合 2026-09-21**：`find_spec` 指向新树、`fruit-dronefly` 0.1.0、仅 1 个 Gym ID、Isaac Sim 5.1 实跑；遗留旧 `DroneFollow` 安装共存（不被新入口导入） | 清理与环境运行前 | `list_envs.py` 实跑输出、spec origin、发行包版本 |
| B02 | OnPolicyRunner／策略类解析、测试图连边与可达路径；见 [[02 策略网络与 PPO 接入]] | 自定义 Actor 接入前 | 安装版 API 符号、选定注册方案、固定图配置和前向验证 |
| B03 | ~~train/play 的 clip_actions 生效值~~ **已闭合 2026-09-21**：`agent.yaml:7 clip_actions: null`，`train.py:199` 与 `play.py:169` 同读 `agent_cfg.clip_actions`，两端一致无额外裁剪 | 动作回归与 PPO 前 | 保存的 agent.yaml、两脚本同一字段 |
| B04 | 安装版环境调用时序、局部 reset；见 [[03 观测动作与控制接口/04 Lee 控制器与执行时序]]。**已闭合 2026-09-21**：100 步计数、局部 reset 两组对照和下一物理步误差均通过；课程分组隔离另见 P12 | 环境验收前 | 调用次数与局部 reset 回归日志 |
| B05 | checkpoint 恢复和实际导出路径；见 [[02 策略网络与 PPO 接入]] | 首版最终验收前 | 图／映射恢复证据、确定性输出与导出数值对比 |
| F01 | FlyVis 安装版本、权重、视觉布局、输出节点及状态；见 [[04 后续升级/01 FlyVis 与相机输入]] | 后续视觉阶段 | 固定版本与来源、形状／空间映射、兼容性验证 |
| F02 | MaleCNS 数据版本、许可、节点身份和子图方向；见 [[04 后续升级/06 FlyDrones 连接组数据适配]] | 后续真实图阶段 | 数据校验值、角色分组、W[post,pre]、路径和循环检查 |
| F03 | 跨模型感觉映射与运动读出；见 [[04 后续升级/07 感觉运动节点映射与可训练读出]] | 后续多模态阶段 | 类型／侧别／空间映射表、群匹配统计、三维可训练读出验证 |
| F04 | 去掉目标真值、循环状态和部署坐标；见 [[04 后续升级/00 升级依赖与实施边界]] | 后续自主导航／部署 | 任务定义、信息依赖检查、序列训练与坐标适配验证 |


## 当前未解决问题

本节集中记录静态审查和代码对照中发现、尚未完成修复或验证的问题。复选框只有在源码变更完成、对应验收执行并留下证据后才可勾选；仅完成文档说明、同一进程重载或短时间探针不能视为解决。每条后面的链接是处理方案、验收条件或问题所在模块，接手 agent 应先跳转阅读再实施。

- [x] **P01：局部 reset 的目标状态与历史隔离。** 2026-09-21 补修完成：选中轨迹计算、噪声及 approach 更新隔离；历史仅由正常 step 推进；课程重新分组保留未重置行。25 步无 reset／频繁 reset 固定噪声对照及 100 步推进计数通过。此前“代码已修”漏查 `_update_target_pose`，本条记录实际补修。证据见 [[03 观测动作与控制接口/04 Lee 控制器与执行时序#2026-09-21 局部 reset 修复与专项验收]]。

- [x] **P02：局部 reset 的动作、控制器和施力缓冲隔离。** 选中行清零并向 composer 传紧凑切片及 env_ids；新增有限范围 slice 处理、取消 reset 全批状态读取。4 环境验证 action_lag=2、重复／空／全量 reset、未重置行及实际 composer 完全不变，下一物理步与无 reset 参考一致（指定容差）。证据同 P01。

- [ ] **P03：Lee 输出的物理限幅和推力可实现性未闭合。**（边界检查已落实，物理标定仍未闭合）已实施部分：`action.py:187` 速度按 `tanh(a)*action_scale` 映射；`action.py:212-214` 推力经 `clamp(min=0.0)` 再 `minimum(_max_thrust)`，`_max_thrust = robot_weight * max_thrust_weight_ratio`（`action.py:72`）；有限值检查共 4 处（`action.py:178/209/215` 与构造期 `34/38/73`），非有限值直接抛 `ValueError`／`FloatingPointError`；控制器侧 `lee_controller_position_and_yaw.py:99` 对 `acc_des_w` 按 `±max_fb_acc`（默认 20.0）夹紧，`:131` 对 `vel_cmd_w` 夹紧，`:39` 拒绝非有限或非正的 `max_fb_acc`。**未完成**：完整力矩物理可实现性标定、陀螺补偿项复核。按首版边界这部分另行处理，保持未勾选。

- [ ] **P04：图 Actor 的 checkpoint 结构、图资产和输入映射恢复规则未实现。** RSL-RL 默认保存参数和优化器，尚未保证模型类型、节点顺序、mask、图版本和映射随 checkpoint 恢复，也未定义版本不匹配时的拒绝行为。方案与导出要求见 [[02 策略网络与 PPO 接入#保存、回放和导出]]，版本接口依据见 [[08 运行环境兼容性与版本方案#对策略实现方案的修正]]。

- [x] **P05：last_action 时间语义。** 100 步实际环境检查中，对未终止环境逐步核对 `last_policy_cmd_scaled` 等于上一步策略命令，reset 清理选中历史。Actor／Critic 仍共用 `last_action_tanh`。证据同 P01；不改变原有时序定义。

- [x] **P06：Python 包重命名已落地。**（2026-09-21 验证）源码树 `source/fruit_dronefly/fruit_dronefly/`；`setup.py` 的 `name="fruit-dronefly"`、`packages=find_packages()`、`package_data={"fruit_dronefly.common.robot.quadcopter": ["*.usd"]}`、`include_package_data=True`；`importlib.util.find_spec("fruit_dronefly").origin` 解析到 `D:\zerozero_code\fruit-dronefly\source\fruit_dronefly\fruit_dronefly\__init__.py`；`importlib.metadata.version("fruit-dronefly")` = 0.1.0；`list_envs.py` 实跑输出配置路径 `fruit_dronefly.tasks.fruit_dronefly.fruit_dronefly_env_cfg:FruitDroneflyEnvCfg`；全仓库文本扫描 0 处旧包／旧任务名残留。**遗留事实**：旧 `DroneFollow` 0.1.0 仍安装、仍可 import（指向 `D:\zerozero_code\dronefollow`），但新入口不导入、不建 alias，符合 [[01 仓库清理与任务收敛#包名统一决策]] 的隔离要求。**未验证**：wheel 构建后 43 MB 级 `drone.usd` 是否确实进入产物——当前仅为配置层证据，按该页警告不等同打包通过。

- [ ] **P07：清理迁移已提交；本轮优化待提交，旧 checkpoint 归档待核查。** 清理前基线 b87f6a2 和清理／Critic／局部 reset 提交 f7f085d 均已存在。当前未提交的是 CUDA 优化及新增测试。此前记录 pretrained/ 未被 Git 跟踪、参考库留存 3 个 .pt 和 3 个 .mp4，尚未单独归档／哈希比对；本轮未补验归档证据，故本项继续未勾选。要求见 [[05 实施顺序与验收#阶段 0：基线与依赖核查]]。

- [ ] **P08：有界规模验证不能代表默认启动或全新安装。** 本轮已完成 2048 环境采样 A/B；headless、无录像、本地材质，自动课程关闭、人工触发部分 reset 与难度变化。仍未验证默认 4096 环境、远程材质／天空资源、全新环境安装及长期运行，不能据此假定可以直接放大。配置和限制见 [[09 自动化测试与回归检查#采样基准与 PPO smoke]]。

- [ ] **P09：首版验收仍有缺口。** 本轮 CPU/CUDA 查表、朝向、轨迹分组、Critic、真实局部 reset 100 步和 64 环境 3 次 PPO smoke 通过，checkpoint 重载确定性输出一致。仍需 play／导出、完整课程行为、heading 往返、默认规模和长期训练验证；短跑不能证明完整 episode、训练收敛或首版全部通过。

- [ ] **P10：真实连接组、FlyVis 与跨模型映射仍是未验证的后续可行性问题。** FlyDrones／MaleCNS 的真实数据、FlyVis 版本和输出节点尚未运行核查；两者不保证节点一一对应，循环图还涉及状态方程、时间尺度、稳定性和序列训练。应先分别完成小规模数据／视觉验证，再做映射闭环。路线边界见 [[04 后续升级/00 升级依赖与实施边界#FlyVis 与真实连接组的统一边界]]、[[04 后续升级/06 FlyDrones 连接组数据适配]]、[[04 后续升级/07 感觉运动节点映射与可训练读出]]、[[04 后续升级/04 循环状态与 PPO 序列训练]]。

- [ ] **P11：当前对照设计不足以单独归因“连接拓扑有效”。** 真实图阶段可能同时改变视觉模型、记忆机制、参数规模和输入映射；当前跟踪任务还使用目标相关 heading、自动偏航和 reset 真值。需要控制可用信息、训练预算、模型规模和任务接口，或把结论限定为工程集成结果。对照边界见 [[05 实施顺序与验收#对照范围]]，自主导航的信息依赖见 [[04 后续升级/02 气味输入与多模态融合#必须同步改变的目标信息]]。

维护规则：解决条目时在对应链接处补充实际代码位置、命令、日期、结果和限制，然后把本条复选框从 `[ ]` 改为 `[x]`；如果只完成部分工作，保持未勾选并更新说明。

## 接手与维护规则

- 先阅读本文、总览和仓库索引，再读取当前任务对应模块；文档无需历史对话补充含义。
- 文档记录“计划做什么”，源码反映“实际做了什么”。两者不符时，记录差异、影响和处理结果；不要悄悄把偏差变成新的需求。
- 当前用户请求决定本次实施范围。读取本库不意味着执行全部升级；已经授权的工作按授权推进。
- 状态分别记录：文档审查（未审查／已静态审查）、代码实施（未实施／实施中／已实施）、运行验证（未执行／未通过／已通过并附证据）。代码写完不等于验收通过。
- 继续保留工程条目的“源码位置、当前行为、计划操作、改后行为、关联影响、验收条件、审查状态”。实施后追加真实位置、版本、结果及证据链接，不覆盖历史失败事实。
- 任何接口、依赖版本或路径变更，都须同步对应模块、仓库索引和验收条目；具体来源只集中维护一处，其他页面链接引用。
- 绝对路径是本机默认位置。新机器先重新定位并记录映射；参考库缺失或版本不同不等于允许自动下载、更新或安装。源码仓库 README／AGENTS 仅指回本库。
- 本库 AGENTS 只有在工具读取相应目录规则时才生效；显式 README 导航始终保留。文档不是未验证命令的执行脚本。

### 注意事项：Windows 中文文档写入

此前多次回填曾因 Windows／PowerShell 到原生命令的编码转换，把中文替换为问号或乱码。后续维护必须使用文件形式传递中文正文，不能重复依赖默认命令管道编码。

- 含中文正文或中文路径的批量修改，先通过支持 UTF-8 的文件编辑工具生成工作区临时 .py 脚本，再执行 python -X utf8 scripts/<临时脚本>.py。不要把含中文的 here-string／正文通过 PowerShell 管道喂给 python -，也不要依赖复杂的 python -c 命令拼接。
- 脚本读取和写入均显式指定 encoding="utf-8"，并保留原文件换行风格。python -X utf8 只控制 Python 端；上游已替换为问号的字符无法恢复，不能仅靠该参数修复管道。
- Python 中 Windows 路径使用原始字符串或正斜杠，避免反斜杠转义产生换页符等控制字符。PowerShell 单独读写时也须明确 UTF-8 编码，不能依赖系统代码页。
- 批量写入前，先读入全部目标文件，在内存中完成替换并验证每个锚点／匹配次数；发现缺失先停止，不让前几个文件已写入而后续文件因锚点失败中断。先核对实际文件内容，不凭终端乱码判定磁盘文件损坏。
- 写入后按 UTF-8 回读，与预期内容逐字比较，复核中文标题、路径、链接、替换字符及异常控制字符。问号也可能是正常标点，应结合原文和预期判断。验证完成后删除临时脚本，记录失败尝试与实际成功范围，不把部分写入描述成“未写入任何文档”。

## 独立阅读自检

| 新 agent 应能回答 | 可独立找到的答案 |
|---|---|
| 项目最终研究什么？ | 本页“工程目标与成功标准” |
| 现在改哪里，参考哪里？ | 本页目录表和仓库索引 |
| 当前已经做了什么？ | 源码清理、包名迁移、局部 reset、CUDA 采样优化和专项验证已完成；图 Actor／play／导出／默认规模仍未验收 |
| 首版做什么、不包含哪些升级？ | 本页决策表、总览与升级边界 |
| 输入如何变成无人机运动？ | 本页接口摘要和四个接口子页 |
| FlyDrones、FlyVis 分别负责什么？ | 决策表、仓库复用矩阵及后续映射页面 |
| 下一步是什么，如何判定完成？ | 阶段 0、B/F 核查项及验收页 |
| 哪些结论尚无运行证据？ | 图 Actor、完整 play／导出、默认 4096 环境、长期训练及未来模块；已通过项见 P12 与验收页 |

## 本页改动记录

- **源码位置**：本页所链接的项目、框架与参考源码；本页本身是文档。
- **当前行为**：源码已收敛为唯一 FruitDronefly-v0，采样热路径完成三项 CUDA 优化；历史对话不作为接手必需输入。
- **计划操作**：在后续获授权的实现中按阶段推进，维护集中上下文与证据。
- **改后行为**：新 agent 可定位工程范围、事实、接口和下一步。
- **关联影响**：两处入口、模块文档、仓库索引与 Canvas。
- **验收条件**：独立阅读自检均有明确答案；链接与状态一致。
- **审查状态**：本页已静态审查；清理、局部 reset 与 CUDA 采样优化已实施并有证据；图 Actor、play／导出和默认规模仍未验证。

## 本轮环境核查进展

见 [[08 运行环境兼容性与版本方案]]：B01 已核对新包解析和注册；B02 的 5.0.1 类解析已确认，图实现未完成；B03 train/play 的 clip_actions=null 已核对；B04 局部 reset 与调用次数专项通过；B05 本轮 MLP checkpoint 重载通过，完整 play／导出和未来图资产恢复仍未完成。

原"暂留包名"方案已被用户确认的统一命名决策取代：Python 包 fruit_dronefly，仓库／发行名 fruit-dronefly，Gym ID IsaacLab-FruitDronefly-v0。详细迁移清单见 [[01 仓库清理与任务收敛#包名统一决策]]。**2026-09-21 更新：该迁移已在源码中完成并实跑通过（见 P06），不再是待办。**

## 2026-09-21 实施与运行证据

| 项目 | 结果 | 依据 |
|---|---|---|
| Gym 注册 | 唯一 `IsaacLab-FruitDronefly-v0`，entry point `isaaclab.envs:ManagerBasedRLEnv` | `list_envs.py` 实跑输出 |
| 包解析 | `find_spec` → 新源码树；`fruit-dronefly` 0.1.0 | Python 探针 |
| 残留引用 | 全仓库文本扫描（含 `.py/.toml/.yaml/.md/.sh`）0 处 | grep 宽域扫描 |
| PPO 训练 | 60 次迭代；`Train/mean_reward` -0.5319 → **0.5519**；`mean_episode_length` 1.88 → **15.48**；`Loss/entropy` 2.12 → -4.00；`learning_rate` 0.0046 → 0.0009（自适应）；`model_0.pt` 已保存 | `events.out.tfevents...`，37 个 scalar tag |
| 策略结构 | `OnPolicyRunner` + `MLPModel`(actor) + `MLPModel`(critic) + `GaussianDistribution` + `PPO`，符合 RSL-RL 5.0.1 独立模型配置 | `params/agent.yaml` |
| 规模与时序 | `num_envs: 64`、`dt: 0.01`、`decimation: 4`、`episode_length_s: 30.0` | `params/env.yaml` |
| 运行环境 | Isaac Sim 5.1、RTX 5060 Laptop 8 GB、cuda:0、Windows 11、headless kit | 终端输出 |

reward 由负转正、episode 长度单调增长、entropy 下降、lr 自适应收缩，这些是**学习正在进行**的证据；60 次迭代（上限 800）不构成收敛或性能结论，也不能作为连接拓扑相关结论的依据。

日志路径：`logs/rsl_rl/fruit_dronefly_v0/2026-09-21_13-34-39/`（含 `params/`、`git/`、tfevents、`model_0.pt`）。

**待办优先级**：本轮优化提交与旧 checkpoint 归档核查（P07）→ play／导出与 heading 往返专项（P09）→ 默认规模和完整课程验证（P08/P09）。P01、P02、B04 与 P12 已有专项证据。

## FlyVis 与真实连接组的关系

本轮建议（待后续可行性核定）：FlyVis 与 MaleCNS 图资产保持分工，无需统一替换成单一模型。FlyVis 是视觉计算来源，MaleCNS 是项目侧连接拓扑来源。统一的是经过版本化、可审查的映射接口和训练边界；首版不接入二者，后续按 [[04 后续升级/00 升级依赖与实施边界#FlyVis 与真实连接组的统一边界]] 的顺序验证。

本次局部 reset 修复与证据入口：[[03 观测动作与控制接口/04 Lee 控制器与执行时序#2026-09-21 局部 reset 修复与专项验收]]。OmniDrones 只读参考，清理与局部 reset 已提交 f7f085d；本轮 CUDA 优化未提交。

## P12：CUDA 采样路径优化

- [x] **P12：reset 批量查表、固定形状 yaw、增量轨迹分组已实施并完成专项验证。** CPU/CUDA 查表 4 项、朝向 12 项、轨迹分组、真实局部 reset 100 步、Critic 观测和 3 次 PPO smoke 均通过。实现位置与性能表见 [[00 改动总览#2026-09-22 CUDA 采样优化实施与证据]]，验收见 [[05 实施顺序与验收#CUDA 采样优化专项（P12）]]。

2048 环境单次有界 A/B 中，partial reset 和 curriculum reset 均值下降 19.9% 和 34.0%；ordinary 均值下降 9.9%，中位数 118.53→118.75 ms 基本不变。局部函数计时不能换算成训练加速倍数；长期吞吐、收敛、默认 4096 环境、远程材质和完整 CUDA kernel timeline 仍未验证。

缓存契约补充：地形创建后，trajectory_params 中的参数字典及其嵌套值视为只读，尤其不能直接修改 start_pos／start_dir。reset 查找表保存的是构建时副本，不会自动失效或跟随字典更新；配置修改应在地形创建前完成，运行期变更需重新创建地形及其依赖命令状态。课程切换只改变索引，不触及该契约。契约已写入 _build_trajectory_reset_tables 的 docstring；此次仅补充说明，未改运行逻辑。
