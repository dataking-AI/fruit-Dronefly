---
tags:
  - fruit-dronefly
  - 工程审查
类型: 工程改动
工程状态: Critic 真值调整已实施；图输入映射未实施
文档审查状态: 已完成本轮静态审查
代码实施状态: Critic 真值调整已实施；图输入映射未实施
运行验证状态: Critic、局部 reset、CUDA 采样和 PPO smoke 专项通过；图 Actor／play／导出未验证
核查日期: 2026-09-22
文档修订: 独立接手版（最小骨架与FlyDrones扩展）
源码根目录: 'D:\zerozero_code\fruit-dronefly'
---

# 观测组成与输入映射

[[07 工程上下文与接手约定|工程上下文与接手约定]] · [[README|阅读入口]]

[[03 观测动作与控制接口/00 接口改动总览|返回接口总览]] · [[00 改动总览|返回总览]]

## 当前 Actor 观测与保留契约

按配置声明顺序拼接；B×19，浮点张量，与策略设备一致。下列索引为左闭右开。

| 索引    | 字段                  |  维度 | 来源和处理                                     |
| ----- | ------------------- | --: | ----------------------------------------- |
| 0:3   | base_lin_vel        |   3 | 自身世界速度转目标 heading；除以 5，再 tanh             |
| 3:7   | base_orientation    |   4 | 相对 heading 的姿态四元数，wxyz；不做速度式缩放            |
| 7:10  | tracking_state 相对位置 |   3 | 目标−无人机，heading 系；除以 5，再 tanh              |
| 10:13 | tracking_state 相对速度 |   3 | 目标速度−无人机速度，heading 系；除以 5，再 tanh          |
| 13:14 | desired_distance    |   1 | 期望跟随水平距离；除以 3，再 tanh                      |
| 14:15 | horizontal_error    |   1 | 实际水平距离−期望距离；除以 3，再 tanh                   |
| 15:16 | z_error             |   1 | 目标与无人机高度差；除以 2，再 tanh                     |
| 16:19 | last_action         |   3 | last_policy_cmd_scaled 中的速度分量；除以 5，再 tanh |

tracking_state 共 9 维，由 _tracking_state_v2_heading_from_target 组装。Actor 的目标参考通过 get_target_state_w(add_noise=True, use_delay=True) 获取。延迟配置 0.11 s，按 0.04 s 环境步量化为 3 步，满历史时约 0.12 s；reset 初期由历史填充处理。

## Critic 的实际来源

Critic 同为 19 维。速度／姿态的 heading 参考和 tracking_state_gt 现在统一使用当前无噪声、无延迟的目标真值。tracking_state_v2_heading 调用 get_target_state_w(add_noise=False, use_delay=False)，相对位置、相对速度、距离／高度误差及其 heading 转换均来自该目标状态。自身速度／姿态来自仿真状态；期望距离仍是任务命令；last_action 仍是实际历史动作。

> [!info] 2026-09-21 用户授权的观测行为调整
> 调整前 tracking_state_v2_heading 读取 command_w 和 command_vel_w，跟踪字段仍带噪声；原文对此的事实判断正确。本次用户明确要求将 Critic 调整为真值输入，属于独立行为变更，不改写为原清理方案。Actor 保持带噪、带延迟路径，19 维排列、归一化、奖励和动作历史不变。未来图 Actor 与 MLP 对照须共用这一 Critic 配置；图 Actor 尚未实施。
>
> 静态编译及专项观测检查已通过：使用实际 ObservationsCfg、观测函数和命令历史读取函数，以两组构造状态验证 B×19、有限值、+X／+Y heading 的解析值、Critic 对当前噪声和两类延迟历史不敏感、真值变化即时生效、Actor 仍响应延迟观测、动作历史一致。检查启动 Isaac Sim 以导入运行依赖，该首次专项当时未创建物理场景、未执行 PPO。后续局部 reset 100 步和本轮 3 次 PPO smoke 均已通过，见本页末尾；历史单项日志仍只代表其当时覆盖范围。
>
> 复现：[check_critic_observations.py](<D:/zerozero_code/fruit-dronefly/scripts/tests/check_critic_observations.py>)；在 env_isaaclab 从源码根目录执行 `python scripts/tests/check_critic_observations.py`。日志：[critic_ground_truth_2026-09-21.log](<D:/zerozero_code/fruit-dronefly/logs/validation/critic_ground_truth_2026-09-21.log>)（本机生成产物，Git 忽略）。已启动的训练不会自动切换；新建训练运行进行对比，旧 checkpoint 的 Critic 输入语义与新配置不同。

## 计划新增的输入映射

测试图输入槽固定为上述 19 个特征，保存“索引→测试图输入节点 ID”表。首版不引入学习型视觉编码器、不复制标量冒充完整嗅觉、不改变现有归一化。测试图节点 ID 不使用未经生物注释支持的 ORN 等命名。

## 改动记录与验收

- **源码位置**：ObservationsCfg；base_lin_vel_w_scaled、base_quat_in_frame、tracking_state_v2_heading*、last_action_tanh。
- **当前行为**：利用已知目标状态生成代理感觉向量。
- **计划操作**：保留字段和顺序，删除 v1 专属观测及不再引用的辅助函数；新增固定输入映射。
- **改后行为**：MLP 与测试图得到相同 19 维信息。
- **关联影响**：图输入尺寸、critic、导出描述、目标延迟历史及 last_action 时序。
- **验收条件**：逐项核对 3+4+9+3=19；Actor/Critic 来源与配置一致；reset 后不混入上一 episode 的目标历史。
- **审查状态**：Critic 真值调整已实施并通过专项观测检查；图输入映射未实施，其他验收项独立维护。

- [ ] 在同一环境状态下比较清理前后观测数值。
- [ ] 验证实际 rollout 时 last_action 的缓冲更新时间，不把字段名称直接当成严格时间下标。
- [ ] 不将这里的目标信息延续到未知气味源任务。

后续替换：[[04 后续升级/01 FlyVis 与相机输入]] · [[04 后续升级/02 气味输入与多模态融合]]。

## 从代理输入到真实感觉节点的升级边界

首版的 19 个输入槽只属于测试图，不给它们强行标注 R1-R6、T4/T5 或 ORN 生物学身份。后续由 [[04 后续升级/07 感觉运动节点映射与可训练读出|显式节点映射]] 替换代理输入。

FlyDrones 的配置可帮助筛选感觉节点群，但 InputEncoder._cells 按数组索引分配视野位置，不满足精确空间对应。正式视觉路线仍采用 FlyVis 布局并校验类型／侧别／空间关系；不增加泊松脉冲转换。气味节点另行依据数据注释选择，不能从 FlyDrones 默认视觉输入表推导。

计划中的图策略改动仍只新增测试图输入映射；本次 Critic 真值调整单列于上文。真实节点转换和新的观测维度属于后续配置版本，见 [[04 后续升级/01 FlyVis 与相机输入]]。

## 已存在源码入口

以下是核查时实际存在的文件；正文中的“拟新增”文件不在此表冒充现状。

| 源码位置（相对代码仓库） | 依据 |
|---|---|
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/fruit_dronefly_env_cfg.py>) | 已存在；2026-09-21 静态核查 |
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/observation.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/observation.py>) | 已存在；2026-09-21 静态核查 |
| [source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/commands/commands.py](<D:/zerozero_code/fruit-dronefly/source/fruit_dronefly/fruit_dronefly/tasks/fruit_dronefly/mdp/commands/commands.py>) | 已存在；2026-09-21 静态核查 |

## 局部 reset 对目标延迟的已知影响

上述约 0.12 s 对应 3 个策略步的历史延迟。修复前，局部 reset 额外追加整批历史，曾缩短其他环境的有效延迟。2026-09-21 已补修：正常步仅追加一次，reset 只重填选中行，不清除其他环境的噪声；课程分组变化保留其他环境的轨迹状态。4 环境、100 步计数和 25 步固定未来噪声的无 reset／频繁 reset 对照通过。具体条件与限制见 [[03 观测动作与控制接口/04 Lee 控制器与执行时序#2026-09-21 局部 reset 修复与专项验收]]。

## 2026-09-22 采样优化对观测契约的影响

reset 查表、轨迹朝向和命令分组刷新均发生在环境采样侧，没有改变 Actor/Critic 的 B×19 排列、缩放、目标真值语义或 last_action 时序。性能 A/B 只反映采样热路径的有界变化，不等于观测质量或训练收敛结论。

本轮已复跑 Critic 专项、真实局部 reset 100 步，并完成 64 环境 3 次 PPO smoke、checkpoint 重载一致性检查。证据见 [[05 实施顺序与验收#CUDA 采样优化专项（P12）]]。
